var ke = Object.defineProperty;
var we = (e, t, n) => t in e ? ke(e, t, { enumerable: !0, configurable: !0, writable: !0, value: n }) : e[t] = n;
var ae = (e, t, n) => (we(e, typeof t != "symbol" ? t + "" : t, n), n);
import b, { openBlock as s, createElementBlock as o, createTextVNode as p, toDisplayString as h, createCommentVNode as d, withDirectives as I, createElementVNode as r, vModelDynamic as Me, Fragment as $, renderList as v, withModifiers as y, vModelText as W, pushScopeId as N, popScopeId as P, normalizeClass as k, vModelSelect as se, createBlock as q, resolveDynamicComponent as V, renderSlot as Ce, resolveComponent as O, createVNode as L, withCtx as Te, createStaticVNode as Ee, resolveDirective as De } from "vue";
import J from "moment";
import Fe from "showdown";
class Ie {
  // Creates a new default log output formatter
  // outputFormat: The output format to use when formatting log events (defaults to "{Timestamp}: [{Level}]: {Message}{Exception}")
  // The following placeholders can be used in the output format:
  // {Timestamp}: The timestamp of the log event
  // {Level}: The log level of the log event
  // {Message}: The message of the log event
  // {Exception}: The exception of the log event
  // {PropertyName}: The properties of the log event (where PropertyName is the name of the property)
  constructor(t) {
    this.outputFormat = t ?? "[{Timestamp}]	[{Level}]	{Message}{Exception}";
  }
  // Formats a log event into a string using the output format
  // event: The log event to format into a string
  // Returns the formatted log event
  format(t) {
    return this.outputFormat.replace(/{(\w+)}/g, (n, a) => a === "Timestamp" ? t.timestamp.toISOString() : a === "Level" ? t.level : a === "Message" ? t.message : a === "Exception" ? t.exception ? `
` + t.exception.stack : "" : t.properties[a]);
  }
}
class qe {
  // Creates a new minimum level log filter
  // minimumLevel: The minimum level to write (defaults to "Debug")
  constructor(t = "Debug") {
    this.allowedLevels = ["Verbose", "Debug", "Information", "Warning", "Error", "Fatal"], this.minimumLevel = t, this.allowedLevels = this.allowedLevels.slice(this.allowedLevels.indexOf(t));
  }
  // Filters a log event and returns true if the event should be written to a sink or false if it should be discarded
  // event: The log event to filter
  // Returns true if the event should be written to a sink or false if it should be discarded
  filter(t) {
    return this.allowedLevels.indexOf(t.level) >= 0;
  }
}
class Ve {
  constructor() {
    this.styles = {
      Verbose: "color: white;",
      Debug: "color: green",
      Information: "color: blue",
      Warning: "color: yellow",
      Error: "color: red",
      Fatal: "color: palevioletred"
    }, this.consoleMethods = {
      Verbose: (t, n, a) => {
        console.log(t, n, a);
      },
      Debug: (t, n, a) => {
        console.debug(t, n, a);
      },
      Information: (t, n, a) => {
        console.info(t, n, a);
      },
      Warning: (t, n, a) => {
        console.warn(t, n, a);
      },
      Error: (t, n, a) => {
        console.error(t, n, a);
      },
      Fatal: (t, n, a) => {
        console.error(t, n, a);
      }
    };
  }
  // Writes a log event to the console
  // event: The log event to write
  write(t) {
    let n = t.args && typeof t.args != "object", a = t.args && typeof t.args == "object";
    this.consoleMethods[t.level]("%c" + t.message, this.styles[t.level], n ? t.args : ""), a && console.table(t.args);
  }
}
class A {
  // Creates a new log sink pipeline
  // loggerConfiguration: The logger configuration that the pipeline belongs to
  constructor(t) {
    this.filters = [], this.enrichers = [], this.loggerConfiguration = t;
  }
  // Sets the sink that the pipeline writes to
  // sink: The sink to write to
  // Returns the logger configuration that the pipeline belongs to
  writeTo(t) {
    return this.sink = t, this.loggerConfiguration;
  }
  // Adds a filter to the pipeline that filters log events by minimum level (lowest level to write)
  // level: The minimum level to write
  // Returns the pipeline
  minimumLevel(t) {
    return this.filters.push(new qe(t)), this;
  }
  // Adds a filter to the pipeline that filters log events before they are written to the sink
  // filter: The filter to add
  // Returns the pipeline
  filter(t) {
    return this.filters.push(t), this;
  }
  // Sets the formatter that the pipeline uses to format log events before they are written to the sink
  // formatter: The formatter to use
  // Returns the pipeline
  formatUsing(t) {
    return this.formatter = t, this;
  }
  // Adds an enricher to the pipeline that enriches log events before they are written to the sink
  // enricher: The enricher to add
  // Returns the pipeline
  enrichWith(t) {
    return this.enrichers.push(t), this;
  }
  // Processes a log event by filtering, enriching and formatting it before writing it to the sink
  // event: The log event to process
  process(t) {
    this.formatter ?? (this.formatter = new Ie()), this.sink ?? (this.sink = new Ve());
    let n = Object.assign({}, t);
    this.filters.some((a) => a.filter(n)) && (this.enrichers.forEach((a) => a.enrich(n)), n.message = this.formatter.format(n) || n.message, this.sink.write(n));
  }
}
class je {
  constructor() {
    this.pipelines = [];
  }
  // Sets the sink that the pipeline writes to
  // sink: The sink to write to
  // Returns the logger configuration that the pipeline belongs to
  writeTo(t) {
    let n = new A(this);
    return this.pipelines.push(n), n.writeTo(t);
  }
  // Adds a filter to the pipeline that filters log events by minimum level (lowest level to write)
  // level: The minimum level to write
  // Returns the pipeline
  minimumLevel(t) {
    let n = new A(this);
    return this.pipelines.push(n), n.minimumLevel(t);
  }
  // Adds a filter to the pipeline that filters log events before they are written to the sink
  // filter: The filter to add
  // Returns the pipeline
  filter(t) {
    let n = new A(this);
    return this.pipelines.push(n), n.filter(t);
  }
  // Sets the formatter that the pipeline uses to format log events before they are written to the sink
  // formatter: The formatter to use
  // Returns the pipeline
  formatUsing(t) {
    let n = new A(this);
    return this.pipelines.push(n), n.formatUsing(t);
  }
  // Adds an enricher to the pipeline that enriches log events before they are written to the sink
  // enricher: The enricher to add
  // Returns the pipeline
  enrichWith(t) {
    let n = new A(this);
    return this.pipelines.push(n), n.enrichWith(t);
  }
  // Writes a log event to the configured sinks after processing it with the configured pipelines
  // level: The level of the log event
  // message: The message of the log event
  // properties: The properties of the log event
  // exception: The exception of the log event
  write(t, n, a, l) {
    let c = {
      level: t,
      message: n,
      properties: {},
      exception: l,
      timestamp: /* @__PURE__ */ new Date(),
      args: a
    };
    this.pipelines.forEach((i) => i.process(c));
  }
}
class f {
  // Hides the constructor
  constructor() {
  }
  // Gets the logger configuration that the logger uses to configure its sinks, filters, formatters and enrichers
  static configure() {
    return this.loggerConfiguration ?? (this.loggerConfiguration = window.LoggerConfiguration || new je()), window.LoggerConfiguration = this.loggerConfiguration, this.loggerConfiguration;
  }
  // Writes a log event to the logger
  // level: The level of the log event
  // message: The message of the log event
  // properties: The properties of the log event
  // exception: The exception of the log event
  static write(t, n, a, l) {
    f.configure().write(t, n, a, l);
  }
  // Writes a log event to the logger with the Verbose level
  // message: The message of the log event
  // properties: The properties of the log event
  static verbose(t, n) {
    this.write("Verbose", t, n);
  }
  // Writes a log event to the logger with the Debug level
  // message: The message of the log event
  // properties: The properties of the log event
  static debug(t, n) {
    this.write("Debug", t, n);
  }
  // Writes a log event to the logger with the Information level
  // message: The message of the log event
  // properties: The properties of the log event
  static information(t, n) {
    this.write("Information", t, n);
  }
  // Writes a log event to the logger with the Warning level
  // message: The message of the log event
  // properties: The properties of the log event
  static warning(t, n) {
    this.write("Warning", t, n);
  }
  // Writes a log event to the logger with the Error level
  // message: The message of the log event
  // properties: The properties of the log event
  // exception: The exception of the log event
  static error(t, n, a) {
    this.write("Error", t, n, a);
  }
  // Writes a log event to the logger with the Fatal level
  // message: The message of the log event
  // properties: The properties of the log event
  // exception: The exception of the log event
  static fatal(t, n, a) {
    this.write("Fatal", t, n, a);
  }
}
class Ae {
  // Constructor
  // dbName: The name of the database
  // tables: The tables to create
  // version: The version of the database
  constructor(t, n, a) {
    this.dbName = t, this.tables = n, this.version = a;
    const l = indexedDB.open(t, a);
    l.onupgradeneeded = (c) => {
      this.database = c.target.result;
      for (const i of n)
        this.database.objectStoreNames.contains(i) && this.database.deleteObjectStore(i), this.database.createObjectStore(i);
    }, l.onsuccess = (c) => {
      this.database = c.target.result;
    }, l.onerror = (c) => {
      f.error("Failed to open the database:", c.target.error);
    };
  }
  // Opens the database connection
  openDatabase() {
    return new Promise((t, n) => {
      const a = indexedDB.open(this.dbName, this.version);
      a.onsuccess = (l) => {
        this.database = l.target.result, t(this);
      }, a.onerror = (l) => {
        f.error("Failed to open the database:", l.target.error), n(new Error("Failed to open the database:" + l.target.error));
      };
    });
  }
  // Adds an object to the database
  // table: The table to add the object to
  // obj: The object to add
  // key: The key to add the object with
  add(t, n, a) {
    return new Promise((l, c) => {
      const m = this.database.transaction(t, "readwrite").objectStore(t).put(n, a);
      m.onsuccess = () => {
        l();
      }, m.onerror = (g) => {
        f.error("Failed to add an object to the database: ", g.target.error), c(new Error("Failed to add an object to the database: " + g.target.error));
      };
    });
  }
  // Removes an object from the database
  // table: The table to remove the object from
  // key: The key of the object to remove
  remove(t, n) {
    return new Promise((a, l) => {
      const u = this.database.transaction(t, "readwrite").objectStore(t).delete(n);
      u.onsuccess = () => {
        a();
      }, u.onerror = (m) => {
        f.error("Failed to remove an object from the database: ", m.target.error), l(new Error("Failed to remove an object from the database: " + m.target.error));
      };
    });
  }
  // Gets an object from the database by key
  // table: The table to get the object from
  // key: The key of the object to get
  getByKey(t, n) {
    return new Promise((a, l) => {
      const u = this.database.transaction(t, "readwrite").objectStore(t).get(n);
      u.onsuccess = (m) => {
        a(m.target.result);
      }, u.onerror = (m) => {
        f.error("Failed to retrieve an object from the database: ", m.target.error), l(new Error("Failed to retrieve an object from the database: " + m.target.error));
      };
    });
  }
  // Gets all objects from the database
  // table: The table to get the objects from
  // query: The query to filter the objects by
  getKeys(t) {
    return new Promise((n, a) => {
      const i = this.database.transaction(t, "readwrite").objectStore(t).getAllKeys();
      i.onsuccess = (u) => {
        n(u.target.result);
      }, i.onerror = (u) => {
        f.error("Failed to retrieve an object from the database: ", u.target.error), a(new Error("Failed to retrieve keys from the database: " + u.target.error));
      };
    });
  }
  // Gets an object from the database by key
  // table: The table to get the object from
  // query: The query to filter the objects by
  get(t, n) {
    return new Promise((a, l) => {
      const u = this.database.transaction(t, "readwrite").objectStore(t).get(n);
      u.onsuccess = (m) => {
        a(m.target.result);
      }, u.onerror = (m) => {
        f.error("Failed to retrieve an object from the database: ", m.target.error), l(new Error("Failed to retrieve an object from the database: " + m.target.error));
      };
    });
  }
  // Gets all objects from the database
  // table: The table to get the objects from
  // query: The query to filter the objects by
  getAll(t, n) {
    return new Promise((a, l) => {
      const u = this.database.transaction(t, "readwrite").objectStore(t).getAll(n);
      u.onsuccess = (m) => {
        a(m.target.result);
      }, u.onerror = (m) => {
        f.error("Failed to retrieve an object from the database: ", m.target.error), l(new Error("Failed to retrieve objects from the database: " + m.target.error));
      };
    });
  }
}
class ie {
  // Constructor
  constructor() {
    this.database = new Ae("cacheStore", ["cache", "cacheEntryOptions"], 1);
  }
  // Clears the database of all items and options
  // Returns a promise that resolves when the operation is complete
  async clear() {
    await this.database.openDatabase();
    let t = await this.database.getAll("cacheEntryOptions"), n = await this.database.getAll("cache");
    for (let a = 0; a < t.length; a++) {
      let l = n[a];
      await this.remove(l.key);
    }
    return this;
  }
  // Gets the options for an object in the database by key
  // key: The key of the object to get the options for
  // Returns a promise that resolves with the options
  async getOptions(t) {
    return await this.database.openDatabase(), this.database.getByKey("cacheEntryOptions", t);
  }
  // Adds an object to the database
  // obj: The object to add
  // key: The key to add the object with
  // options: The options to add the object with
  async add(t, n, a) {
    return await this.database.openDatabase(), await this.database.add("cache", t, n), await this.database.add("cacheEntryOptions", a, n), this;
  }
  // Removes an object from the database
  // key: The key of the object to remove
  async remove(t) {
    return await this.database.openDatabase(), await this.database.remove("cache", t), await this.database.remove("cacheEntryOptions", t), this;
  }
  // Gets an object from the database by key
  // table: The table to get the object from
  // key: The key of the object to get
  async get(t) {
    return await this.database.openDatabase(), this.database.getByKey("cache", t);
  }
  // Compacts the database. This is used to remove expired items from the database.
  // This method is called automatically by the cache.
  // Returns a promise that resolves when the operation is complete
  async compact() {
    await this.database.openDatabase();
    let t = await this.database.getAll("cacheEntryOptions"), n = await this.database.getAll("cache"), a = (/* @__PURE__ */ new Date()).getTime();
    for (let l = 0; l < t.length; l++) {
      let c = t[l], i = n[l];
      c.expirationTime != 0 && c.expirationTime < a && await this.remove(i.key);
    }
    return this;
  }
}
class Q {
  // Hides the constructor
  constructor() {
  }
  // Configures the cache with the specified storage provider
  // storageProvider: The storage provider to use for the cache
  static configure(t = new ie()) {
    this.storageProvider ?? (this.storageProvider = window.StorageProvider || new ie()), window.StorageProvider = this.storageProvider;
  }
  // Sets an object in the cache by key with the specified options
  // key: The key to set the object with
  // value: The value to set the object with
  // entryOptions: The options to set the object with
  // Returns a promise that resolves when the operation is complete
  static async set(t, n, a = { expirationTime: 0, slidingExpirationTime: 0, sliding: !1 }) {
    f.debug("Setting object in cache: ", { key: t, value: n, entryOptions: a }), this.configure(), await this.storageProvider.add(n, t, a);
  }
  // Gets an object from the cache by key and resets the expiration time if the object is set to sliding expiration
  // key: The key of the object to get
  // Returns a promise that resolves with the object
  static async get(t) {
    f.debug("Getting object from cache: " + t), this.configure(), await this.storageProvider.compact();
    let n = await this.storageProvider.get(t);
    if (n == null)
      return n;
    let a = await this.storageProvider.getOptions(t);
    return a.sliding && await this.set(t, n, {
      expirationTime: (/* @__PURE__ */ new Date()).getTime() + a.slidingExpirationTime,
      slidingExpirationTime: a.slidingExpirationTime,
      sliding: !0
    }), n;
  }
  // Removes an object from the cache by key
  // key: The key of the object to remove
  // Returns a promise that resolves when the operation is complete
  static async remove(t) {
    f.debug("Removing object from cache: " + t), this.configure(), await this.storageProvider.remove(t);
  }
  // Clears the cache of all items
  // Returns a promise that resolves when the operation is complete
  static async clear() {
    f.debug("Clearing cache"), this.configure(), await this.storageProvider.clear();
  }
}
class Oe {
}
class w {
  // Constructor
  // options: The request options
  constructor(t) {
    this.options = {
      method: "GET",
      url: "",
      headers: {},
      credentials: "same-origin",
      serializer: JSON.stringify,
      parser: (n) => n.json(),
      success: (n) => {
        f.debug("Request response from " + this.options.url + ":", n);
      },
      error: (n) => {
        f.error("Request error from " + this.options.url + ":", n);
      },
      retry: (n) => {
        f.debug("Request retry on " + this.options.url + ":", { attempt: n });
      },
      storageMode: 0,
      cacheKey: "",
      timeout: 6e4,
      retryAttempts: 3,
      retryDelay: 1e3
    }, this.abortController = null, this.options = { ...this.options, ...t };
  }
  // Creates a GET request
  // Note: GET requests are cached by default
  // url: The request url
  // data: The request data
  static get(t, n) {
    return new w({ method: "GET", url: t, data: n, cacheKey: t + JSON.stringify(n) }).withHeaders({
      Accept: "application/json"
    });
  }
  // Creates a POST request
  // Note: POST requests are not cached by default
  // url: The request url
  // data: The request data
  static post(t, n) {
    return new w({
      method: "POST",
      url: t,
      data: n,
      cacheKey: t + JSON.stringify(n),
      storageMode: 2
      /* NetworkOnly */
    }).withHeaders({
      "Content-Type": "application/json",
      Accept: "application/json"
    });
  }
  // Creates a PUT request
  // Note: PUT requests are not cached by default
  // url: The request url
  // data: The request data
  static put(t, n) {
    return new w({
      method: "PUT",
      url: t,
      data: n,
      cacheKey: t + JSON.stringify(n),
      storageMode: 2
      /* NetworkOnly */
    }).withHeaders({
      "Content-Type": "application/json",
      Accept: "application/json"
    });
  }
  // Creates a DELETE request
  // Note: DELETE requests are not cached by default
  // url: The request url
  // data: The request data
  static delete(t, n) {
    return new w({
      method: "DELETE",
      url: t,
      data: n,
      cacheKey: t + JSON.stringify(n),
      storageMode: 2
      /* NetworkOnly */
    }).withHeaders({
      Accept: "application/json"
    });
  }
  // Creates a request of the specified type (e.g. GET, POST, PUT, DELETE, etc.)
  // method: The request method
  // url: The request url
  // data: The request data
  static ofType(t, n, a) {
    return new w({ method: t, url: n, data: a, cacheKey: n + JSON.stringify(a) }).withHeaders({
      Accept: "application/json"
    });
  }
  // Adds an authentication provider to the request (used to authenticate the request)
  // authenticationProvider: The authentication provider
  withAuthenticationProvider(t) {
    return this.options.authenticationProvider = t, this;
  }
  // Adds header values to the request
  // headers: The header values
  withHeaders(t) {
    return this.options.headers = { ...this.options.headers, ...t }, this;
  }
  // Adds a cancellation token to the request (used to cancel the request)
  // cancellationToken: The cancellation token
  // Note: The request will finish executing, but the success/error callbacks will not be called if the request is cancelled.
  withCancellationToken(t) {
    return this.options.cancellationToken = t, this;
  }
  // Adds credentials to the request
  // credentials: The credentials
  withCredentials(t) {
    return this.options.credentials = t, this;
  }
  // Sets the serializer for the request
  // serializer: The serializer
  withSerializer(t) {
    return this.options.serializer = t, this;
  }
  // Sets the parser for the request
  // parser: The parser
  withParser(t) {
    return this.options.parser = t, this;
  }
  // Sets the success callback for the request
  // callback: The success callback
  onSuccess(t) {
    return this.options.success = t ?? ((n) => {
      f.debug("Request response:", n);
    }), this;
  }
  // Sets the error callback for the request
  // callback: The error callback
  onError(t) {
    return this.options.error = t ?? ((n) => {
      f.error("Request error:", n);
    }), this;
  }
  // Sets the retry callback for the request
  // callback: The retry callback
  onRetry(t) {
    return this.options.retry = t ?? ((n) => {
      f.debug("Request retry:", { attempt: n });
    }), this;
  }
  // Sets the storage mode for the request
  // storageMode: The storage mode
  withStorageMode(t) {
    return this.options.storageMode = t, this;
  }
  // Sets the cache key for the request
  // cacheKey: The cache key
  withCacheKey(t) {
    return this.options.cacheKey = t, this;
  }
  // Sets the timeout for the request
  // timeout: The timeout in milliseconds (default: 60000)
  // Note: The timeout is only used for network requests
  withTimeout(t) {
    return this.options.timeout = t ?? 6e4, this;
  }
  // Sets the number of retry attempts for the request
  // retryAttempts: The number of retry attempts (default: 3)
  // Note: The retry attempts are only used for network requests
  withRetryAttempts(t) {
    return this.options.retryAttempts = t, this;
  }
  // Sets the retry delay for the request in milliseconds
  // retryDelay: The retry delay in milliseconds (default: 1000)
  // Note: The retry delay is only used for network requests
  withRetryDelay(t) {
    return this.options.retryDelay = t, this;
  }
  // Aborts the request, if it is still running, and calls the error callback.
  // Note: This is only supported for network requests
  abort() {
    return this.abortController === null ? this : (this.abortController.abort(), this.options.error(new Error("The request was aborted.")), this);
  }
  // Actually sends the request, parses it, and calls either the
  // success or error functions if they exist.
  // Returns the parsed response.
  async send() {
    const { authenticationProvider: t, method: n, url: a, data: l, headers: c, credentials: i, serializer: u, parser: m, success: g, error: M, storageMode: F, cacheKey: H, timeout: be, retryAttempts: Se, retryDelay: $e, retry: ve, cancellationToken: K } = this.options, te = new AbortController();
    this.abortController = te;
    let G = 0, U = null;
    const ne = async () => {
      if (F === 1 || F === 3) {
        const C = await Q.get(H);
        if (C !== void 0 && (g(C), F === 1))
          return C;
      }
      if (!navigator.onLine) {
        if (F === 0) {
          const T = await Q.get(H);
          if (T !== void 0)
            return g(T), T;
        }
        const C = new Error("System is offline");
        return M(C), Promise.reject(C);
      }
      try {
        const C = u(l);
        await (t == null ? void 0 : t.authenticate(this));
        const T = await Promise.race([
          fetch(a, {
            method: n,
            credentials: i,
            headers: c,
            body: C,
            signal: te.signal
          }),
          this.handleTimeout(be)
        ]);
        if (K != null && K.canceled)
          return Promise.reject(new Error("The request was canceled."));
        if (T.status >= 200 && T.status < 300) {
          const _ = await m(T);
          return g(_), F !== 2 && await Q.set(H, _), _;
        }
        U = new Error(T.statusText), T.status === 401 && await (t == null ? void 0 : t.authenticationFailed(this, T));
      } catch (C) {
        U = C;
      }
      return G < Se ? (++G, await new Promise((C) => setTimeout(C, $e)), ve(G), ne()) : (M(U), Promise.reject(U));
    };
    return ne();
  }
  // Handles the timeout for the request
  // timeout: The timeout in milliseconds (default: 60000)
  // returns: A promise that rejects when the timeout is reached
  async handleTimeout(t) {
    throw t ?? (t = 6e4), await new Promise((n, a) => {
      setTimeout(() => {
        a(new Error("Request timeout"));
      }, t);
    }), new Error("Request timeout");
  }
}
var j = /* @__PURE__ */ ((e) => (e[e.NetworkFirst = 0] = "NetworkFirst", e[e.StorageFirst = 1] = "StorageFirst", e[e.NetworkOnly = 2] = "NetworkOnly", e[e.StorageAndUpdate = 3] = "StorageAndUpdate", e))(j || {});
String.prototype.slugify = function() {
  return this.trim().replace(/ /g, "-").replace(/-{2,}/g, "-").replace(/^-+|-+$/g, "").replace(/([^a-zA-Z0-9-_/./:]+)/g, "");
};
String.prototype.stripHTML = function() {
  let e = document.createElement("div");
  return e.innerHTML = this, (e.textContent || e.innerText || "").replace(/^\s+|\s+$/g, "");
};
String.prototype.toNumber = function() {
  return this == null ? 0 : parseFloat(this.stripHTML().replace(/[^0-9.-]/g, ""));
};
function z(e, t = 300) {
  let n;
  return function(...a) {
    clearTimeout(n), n = setTimeout(() => {
      e.apply(this, a);
    }, t);
  };
}
class D {
}
class B {
  constructor() {
    this.name = "inputElement";
  }
  // The validation method. This method should return a promise that resolves to true if the value is valid, or false if it is not.
  // value: The value to validate
  // params: Any additional parameters to pass to the validation method
  // Returns a promise that resolves to true if the value is valid, or false if it is not.
  validate(t, ...n) {
    let a = t;
    return a == null ? new Promise((l, c) => {
      l({ isValid: !0, errorMessage: "" });
    }) : new Promise((l, c) => {
      a.checkValidity() ? l({ isValid: !0, errorMessage: a.validationMessage }) : l({ isValid: !1, errorMessage: a.validationMessage });
    });
  }
}
class Z {
  constructor() {
    this.name = "selectElement";
  }
  // The validation method. This method should return a promise that resolves to true if the value is valid, or false if it is not.
  // value: The value to validate
  // params: Any additional parameters to pass to the validation method
  // Returns a promise that resolves to true if the value is valid, or false if it is not.
  validate(t, ...n) {
    let a = t;
    return a == null ? new Promise((l, c) => {
      l({ isValid: !0, errorMessage: "" });
    }) : new Promise((l, c) => {
      a.checkValidity() ? l({ isValid: !0, errorMessage: a.validationMessage }) : l({ isValid: !1, errorMessage: a.validationMessage });
    });
  }
}
class oe {
  constructor() {
    this.name = "textAreaElement";
  }
  // The validation method. This method should return a promise that resolves to true if the value is valid, or false if it is not.
  // value: The value to validate
  // params: Any additional parameters to pass to the validation method
  // Returns a promise that resolves to true if the value is valid, or false if it is not.
  validate(t, ...n) {
    let a = t;
    return a == null ? new Promise((l, c) => {
      l({ isValid: !0, errorMessage: "" });
    }) : new Promise((l, c) => {
      a.checkValidity() ? l({ isValid: !0, errorMessage: a.validationMessage }) : l({ isValid: !1, errorMessage: a.validationMessage });
    });
  }
}
class Le {
  // Constructor - private to prevent instantiation
  constructor() {
  }
  // Initializes the validation rules
  static initialize() {
    this.rules ?? (this.rules = window.ValidationConfiguration || {}), window.ValidationConfiguration = this.rules;
  }
  // Adds a validation rule group to the list of validation rules
  // name: The name of the validation rule group
  // group: The validation rule group to add
  static addRuleGroup(t, n) {
    this.initialize(), this.rules[t] = n;
  }
  // Validates an object against a validation rule group
  // obj: The object to validate
  // ruleGroup: The name of the validation rule group to validate against
  // errors: An optional object to populate with errors if the object is invalid (the key is the property name, the value is an array of error messages) (optional)
  // Returns a promise that resolves to true if the object is valid, or false if it is not
  static async validate(t, n, a = {}) {
    this.initialize();
    let l = !0, c = this.rules[n];
    if (!c)
      return l;
    for (let i in c) {
      if (!c.hasOwnProperty(i))
        continue;
      let u = t[i], m = c[i];
      for (let g = 0; g < m.length; g++) {
        let F = await m[g].validate(u);
        F.isValid || (l = !1, a.hasOwnProperty(i) ? a[i].push(F.errorMessage) : a[i] = [F.errorMessage]);
      }
    }
    return l;
  }
  // Validates a form
  // form: The form to validate
  // errors: An optional object to populate with errors if the form is invalid (the key is the property name, the value is an array of error messages) (optional)
  // ruleGroup: The name of the validation rule group to validate against (optional)
  // Returns a promise that resolves to true if the form is valid, or false if it is not
  static async validateForm(t, n = null, a = {}) {
    this.initialize(), n ?? (n = t.getAttribute("data-validation-rule-group")), n || (n = this.deriveObjectType(t));
    let l = this.rules[n];
    l || (l = this.extractRulesFromForm(t), this.addRuleGroup(n, l));
    const c = this.extractFormData(t);
    return this.validate(c, n, a);
  }
  // Extracts the form data from a form and returns it as an array
  // form: The form to extract the data from
  // Returns an array of form data
  static extractFormData(t) {
    let n = {}, a = t.getElementsByTagName("input"), l = t.getElementsByTagName("textarea"), c = t.getElementsByTagName("select");
    for (let i = 0; i < a.length; i++)
      n[a[i].name] = a[i];
    for (let i = 0; i < l.length; i++)
      n[l[i].name] = l[i];
    for (let i = 0; i < c.length; i++)
      n[c[i].name] = c[i];
    return n;
  }
  // Derives the object type from a form element (if no object type is specified)
  // form: The form to derive the object type from
  // Returns the object type
  static deriveObjectType(t) {
    return t.action || t.id || t.name;
  }
  // Extracts the validation rules from a form and adds them to the list of validation rules
  // form: The form to extract the validation rules from
  // Returns the validation rule group that was extracted
  static extractRulesFromForm(t) {
    var i, u, m;
    let n = {}, a = t.getElementsByTagName("input"), l = t.getElementsByTagName("textarea"), c = t.getElementsByTagName("select");
    for (let g = 0; g < a.length; ++g) {
      let M = a[g];
      M.willValidate && (n[i = M.name] ?? (n[i] = []), n[M.name].push(new B()));
    }
    for (let g = 0; g < l.length; ++g) {
      let M = l[g];
      M.willValidate && (n[u = M.name] ?? (n[u] = []), n[M.name].push(new oe()));
    }
    for (let g = 0; g < c.length; ++g) {
      let M = c[g];
      M.willValidate && (n[m = M.name] ?? (n[m] = []), n[M.name].push(new Z()));
    }
    return n;
  }
}
const Ne = b.defineComponent({
  name: "form-field-input",
  data: function() {
    return {
      internalModel: this.formatValue(this.model),
      internalSchema: this.schema,
      dataListQuery: "query($queryType: String!, $queryFilter: String!) { dropDown(type: $queryType, filter: $queryFilter) { key, value } }",
      errorMessage: ""
    };
  },
  props: {
    model: {
      type: String,
      required: !0,
      default: ""
    },
    schema: {
      type: D,
      required: !0,
      default: () => new D()
    },
    showLabel: {
      type: Boolean,
      required: !1,
      default: !0
    }
  },
  methods: {
    // determines if the field should be validated
    willValidate: function() {
      return this.internalSchema.metadata.required || this.internalSchema.metadata.maxlength || this.internalSchema.metadata.minlength || this.internalSchema.metadata.pattern || this.internalSchema.metadata.min || this.internalSchema.metadata.max;
    },
    // revalidates the field
    revalidate: async function() {
      let e = document.getElementById(this.getFieldID());
      if (e == null)
        return;
      let t = await new B().validate(e);
      return this.errorMessage = t.errorMessage, t.isValid;
    },
    // converts the value to a date
    // value: the value to convert
    convertToDate: function(e) {
      return this.schema.metadata.isUTC ? J.utc(e || /* @__PURE__ */ new Date()).local() : J(e || /* @__PURE__ */ new Date());
    },
    // formats the value to the correct format
    // value: the value to format
    formatValue: function(e) {
      return e && (this.schema.metadata.inputType === "date" ? this.convertToDate(e).format("YYYY-MM-DD") : this.schema.metadata.inputType === "datetime-local" || this.schema.metadata.inputType === "datetime" ? this.convertToDate(e).format("YYYY-MM-DDTHH:mm") : this.schema.metadata.inputType === "month" ? this.convertToDate(e).format("YYYY-MM") : e);
    },
    // gets the field id
    getFieldID: function() {
      return this.internalSchema.propertyName.slugify() + this.internalSchema.key;
    },
    // Called when the model changes
    // newValue: the new value
    // emits a changed event
    changed: function(e) {
      this.updateDataList(), this.revalidate(), this.$emit("changed", e, this.schema);
    },
    // Gets the datalist id
    getList: function() {
      return this.schema.metadata.datalist ? this.getFieldID() + "-list" : null;
    },
    // Updates the datalist for the field
    updateDataList: z(async function() {
      if (!this.internalSchema.queryType)
        return;
      let e = this;
      w.post("/api/query", {
        query: e.datalistQuery,
        variables: {
          queryType: e.internalSchema.metadata.queryType,
          queryFilter: e.internalModel
        }
      }).onSuccess((t) => {
        f.debug("Select: " + e.internalSchema.metadata.queryType + " loaded successfully.", t.data.dropDown), e.internalSchema.metadata.datalist = t.data.dropDown;
      }).onError((t) => {
        f.error("Select: " + e.internalSchema.metadata.queryType + " failed to load.", t);
      }).withStorageMode(j.StorageAndUpdate).send();
    }, 300)
  },
  // created method, updates the datalist values
  created: function() {
    this.updateDataList();
  },
  watch: {
    // watches the model and updates the internal model
    // newModel: the new model
    // oldModel: the old model
    model: function(e, t) {
      t !== e && (this.internalModel = e, this.$nextTick(function() {
        this.revalidate();
      }));
    },
    // watches the schema and updates the internal schema
    // newSchema: the new schema
    // oldSchema: the old schema
    schema: function(e, t) {
      t !== e && (this.internalSchema = e, this.$nextTick(function() {
        this.revalidate();
      }));
    }
  },
  // mounted method, revalidates the field
  mounted: function() {
    this.$nextTick(function() {
      this.revalidate();
    });
  }
}), S = (e, t) => {
  const n = e.__vccOpts || e;
  for (const [a, l] of t)
    n[a] = l;
  return n;
}, Pe = ["for"], Be = {
  key: 0,
  class: "error clear-background"
}, Re = {
  key: 1,
  class: "clear-background active no-border small"
}, Ue = /* @__PURE__ */ r("span", { class: "fas fa-info-circle" }, null, -1), ze = {
  key: 2,
  class: "error clear-background"
}, Ye = {
  key: 3,
  class: "success clear-background fas fa-check-circle"
}, He = { key: 1 }, Ke = ["id", "type", "disabled", "accept", "alt", "autocomplete", "checked", "dirname", "formaction", "formenctype", "formmethod", "formnovalidate", "formtarget", "height", "list", "max", "maxlength", "min", "minlength", "multiple", "name", "pattern", "placeholder", "title", "readonly", "required", "size", "src", "step", "width", "files", "data-error-message-value-missing", "data-error-message-pattern-mismatch", "data-error-message-range-overflow", "data-error-message-range-underflow", "data-error-message-step-mismatch", "data-error-message-too-long", "data-error-message-too-short", "data-error-message-bad-input", "data-error-message-type-mismatch"], Ge = {
  key: 2,
  class: "text-center"
}, _e = ["id"], Qe = ["value"];
function Je(e, t, n, a, l, c) {
  return s(), o("div", null, [
    e.showLabel && e.internalSchema.displayName ? (s(), o("label", {
      key: 0,
      for: e.getFieldID()
    }, [
      p(h(e.internalSchema.displayName) + " ", 1),
      e.internalSchema.metadata.required && e.errorMessage ? (s(), o("span", Be, "*")) : d("", !0),
      e.internalSchema.metadata.hint ? (s(), o("i", Re, [
        Ue,
        p(h(e.internalSchema.metadata.hint), 1)
      ])) : d("", !0),
      e.errorMessage ? (s(), o("span", ze, h(e.errorMessage), 1)) : d("", !0),
      !e.errorMessage && e.willValidate() ? (s(), o("span", Ye)) : d("", !0)
    ], 8, Pe)) : d("", !0),
    e.internalSchema.metadata.subtitle ? (s(), o("div", He, h(e.internalSchema.metadata.subtitle), 1)) : d("", !0),
    I(r("input", {
      id: e.getFieldID(),
      type: e.internalSchema.metadata.inputType,
      "onUpdate:modelValue": t[0] || (t[0] = (i) => e.internalModel = i),
      onInput: t[1] || (t[1] = (i) => e.changed(i.target.value)),
      disabled: e.internalSchema.metadata.disabled,
      accept: e.internalSchema.metadata.accept,
      alt: e.internalSchema.metadata.alt,
      autocomplete: e.internalSchema.metadata.autocomplete,
      checked: e.internalSchema.metadata.checked,
      dirname: e.internalSchema.metadata.dirname,
      formaction: e.internalSchema.metadata.formaction,
      formenctype: e.internalSchema.metadata.formenctype,
      formmethod: e.internalSchema.metadata.formmethod,
      formnovalidate: e.internalSchema.metadata.formnovalidate,
      formtarget: e.internalSchema.metadata.formtarget,
      height: e.internalSchema.metadata.height,
      list: e.getList(),
      max: e.internalSchema.metadata.max,
      maxlength: e.internalSchema.metadata.maxlength,
      min: e.internalSchema.metadata.min,
      minlength: e.internalSchema.metadata.minlength,
      multiple: e.internalSchema.metadata.multiple,
      name: e.getFieldID(),
      pattern: e.internalSchema.metadata.pattern,
      placeholder: e.internalSchema.metadata.placeholder,
      title: e.internalSchema.metadata.placeholder,
      readonly: e.internalSchema.metadata.readonly,
      required: e.internalSchema.metadata.required,
      size: e.internalSchema.metadata.size,
      src: e.internalSchema.metadata.src,
      step: e.internalSchema.metadata.step,
      width: e.internalSchema.metadata.width,
      files: e.internalSchema.metadata.files,
      "data-error-message-value-missing": e.internalSchema.metadata.errorMessageValueMissing,
      "data-error-message-pattern-mismatch": e.internalSchema.metadata.errorMessagePatternMismatch,
      "data-error-message-range-overflow": e.internalSchema.metadata.errorMessageRangeOverflow,
      "data-error-message-range-underflow": e.internalSchema.metadata.errorMessageRangeUnderflow,
      "data-error-message-step-mismatch": e.internalSchema.metadata.errorMessageStepMismatch,
      "data-error-message-too-long": e.internalSchema.metadata.errorMessageTooLong,
      "data-error-message-too-short": e.internalSchema.metadata.errorMessageTooShort,
      "data-error-message-bad-input": e.internalSchema.metadata.errorMessageBadInput,
      "data-error-message-type-mismatch": e.internalSchema.metadata.errorMessageTypeMismatch
    }, null, 40, Ke), [
      [Me, e.internalModel]
    ]),
    e.internalSchema.metadata.inputType === "color" || e.internalSchema.metadata.inputType === "range" ? (s(), o("div", Ge, h(e.internalModel), 1)) : d("", !0),
    e.internalSchema.metadata.datalist ? (s(), o("datalist", {
      key: 3,
      id: e.getList()
    }, [
      (s(!0), o($, null, v(e.internalSchema.metadata.datalist, (i) => (s(), o("option", {
        value: i.value,
        key: i.key
      }, null, 8, Qe))), 128))
    ], 8, _e)) : d("", !0)
  ]);
}
const le = /* @__PURE__ */ S(Ne, [["render", Je]]);
class X {
  // Returns the current domain.
  static get domain() {
    return window.location.protocol + "//" + window.location.host + "/";
  }
  // Determines if this is being run locally or in production.
  static get isLocal() {
    return /^http:\/\/localhost:\d{5}\/$/.test(X.domain);
  }
  // Gets the hash without the hash bang.
  static get HashBang() {
    return window.location.hash.replace("#!", "");
  }
  // Gets the text after the last slash. Presumably the ID needed.
  static get Id() {
    return window.location.pathname.substring(window.location.pathname.lastIndexOf("/") + 1);
  }
  // Gets a value from the query string.
  static GetQueryString(t) {
    let n = window.location.href;
    var a = new RegExp("[?&]" + t + "=([^&#]*)", "i"), l = a.exec(n);
    return l ? l[1] : null;
  }
  // Sets the title for the page.
  static setPageTitle(t) {
    document.title = t;
  }
}
const We = b.defineComponent({
  name: "form-field-html",
  data: function() {
    return {
      internalModel: this.model,
      internalSchema: this.schema,
      errorMessage: ""
    };
  },
  props: {
    model: {
      type: String,
      required: !0,
      default: ""
    },
    schema: {
      type: D,
      required: !0,
      default: () => new D()
    },
    showLabel: {
      type: Boolean,
      required: !1,
      default: !0
    }
  },
  computed: {
    compiledMarkdown: function() {
      return this.markdown(this.currentContent);
    }
  },
  methods: {
    // determines if the field should be validated
    willValidate: function() {
      return this.internalSchema.metadata.required || this.internalSchema.metadata.maxlength || this.internalSchema.metadata.minlength || this.internalSchema.metadata.pattern || this.internalSchema.metadata.min || this.internalSchema.metadata.max;
    },
    // revalidates the field
    revalidate: async function() {
      let e = document.getElementById(this.getFieldID());
      if (e == null)
        return;
      let t = await new oe().validate(e);
      return this.errorMessage = t.errorMessage, t.isValid;
    },
    // gets the field id
    getFieldID: function() {
      return this.internalSchema.propertyName.slugify() + this.internalSchema.key;
    },
    showFrame: function(e) {
      this.displayFrame = !0, e === "image" ? this.iframeSrc = this.iframeImageSrc : this.iframeSrc = this.iframeFileSrc;
    },
    markdown: function(e) {
      if (!e)
        return e;
      var t = new Fe.Converter({ tables: !0, strikethrough: !0, emoji: !0, underline: !0, ghMentions: !0, ghMentionsLink: X.domain + "People/{u}" });
      return t.makeHtml(e);
    },
    keyup: function() {
      this.$emit("keyup", {
        content: this.currentContent
      });
    },
    cancel: function() {
      this.$emit("cancel");
    },
    tag: function(e) {
      let t = document.getElementById("editorInput");
      if (!t)
        return;
      let n = t.selectionStart, a = t.selectionEnd, l = e + this.currentContent.substring(n, a);
      this.currentContent = this.currentContent.substring(0, n) + l + this.currentContent.substring(a, this.currentContent.length), t.focus();
    },
    symmetricTag: function(e) {
      let t = document.getElementById("editorInput");
      if (!t)
        return;
      let n = t.selectionStart, a = t.selectionEnd, l = e + this.currentContent.substring(n, a) + e;
      this.currentContent = this.currentContent.substring(0, n) + l + this.currentContent.substring(a, this.currentContent.length), t.focus();
    },
    linkTag: function(e) {
      let t = document.getElementById("editorInput");
      if (!t)
        return;
      let n = t.selectionStart, a = t.selectionEnd, l = "[" + this.currentContent.substring(n, a) + "](" + e + ")";
      this.currentContent = this.currentContent.substring(0, n) + l + this.currentContent.substring(a, this.currentContent.length), t.focus();
    },
    imageTag: function(e) {
      let t = document.getElementById("editorInput");
      if (!t)
        return;
      let n = t.selectionStart, a = t.selectionEnd, l = "![" + this.currentContent.substring(n, a) + "](" + e + ")";
      this.currentContent = this.currentContent.substring(0, n) + l + this.currentContent.substring(a, this.currentContent.length), t.focus();
    }
  },
  watch: {
    // watches the model and updates the internal model
    // newModel: the new model
    // oldModel: the old model
    model: function(e, t) {
      t !== e && (this.internalModel = e, this.$nextTick(function() {
        this.revalidate();
      }));
    },
    // watches the schema and updates the internal schema
    // newSchema: the new schema
    // oldSchema: the old schema
    schema: function(e, t) {
      t !== e && (this.internalSchema = e, this.$nextTick(function() {
        this.revalidate();
      }));
    }
  },
  // mounted is called when the component is loaded
  // this is where we setup the validation rules and the event listeners
  mounted: function() {
    let e = this;
    window.addEventListener("message", function(t) {
      if (typeof t.data != "string")
        return;
      let n = JSON.parse(t.data);
      e.displayFrame = !1, n.href && (e.iframeSrc == e.iframeImageSrc ? e.imageTag(n.href) : e.linkTag(n.href));
    }), this.$nextTick(function() {
      this.revalidate();
    });
  }
});
const R = (e) => (N("data-v-aabc8531"), e = e(), P(), e), Ze = { class: "flex row" }, Xe = {
  class: "flex-item flex justify-space-between column wrap",
  id: "editorRawInput"
}, xe = { class: "menu pill flex-item" }, et = { class: "item has-children" }, tt = /* @__PURE__ */ R(() => /* @__PURE__ */ r("a", { href: "#!" }, "Header", -1)), nt = { class: "item has-children" }, at = /* @__PURE__ */ R(() => /* @__PURE__ */ r("a", { href: "#!" }, "Format", -1)), it = { class: "item has-children" }, rt = /* @__PURE__ */ R(() => /* @__PURE__ */ r("a", { href: "#!" }, "List", -1)), st = { class: "item has-children" }, ot = /* @__PURE__ */ R(() => /* @__PURE__ */ r("a", { href: "#!" }, "File", -1)), lt = /* @__PURE__ */ R(() => /* @__PURE__ */ r("br", null, null, -1)), dt = {
  key: 0,
  class: "flex-item",
  id: "editorCompiledInput"
}, ct = ["innerHTML"], ut = {
  key: 1,
  class: "iframe-holder z-depth-2"
}, ht = ["src"];
function mt(e, t, n, a, l, c) {
  return s(), o("div", Ze, [
    r("div", Xe, [
      r("nav", xe, [
        r("ul", null, [
          r("li", et, [
            tt,
            r("ul", null, [
              r("li", null, [
                r("h1", null, [
                  r("a", {
                    href: "#!",
                    onClick: t[0] || (t[0] = y((i) => e.tag("#"), ["stop", "prevent"]))
                  }, "Header 1")
                ])
              ]),
              r("li", null, [
                r("h2", null, [
                  r("a", {
                    href: "#!",
                    onClick: t[1] || (t[1] = y((i) => e.tag("##"), ["stop", "prevent"]))
                  }, "Header 2")
                ])
              ]),
              r("li", null, [
                r("h3", null, [
                  r("a", {
                    href: "#!",
                    onClick: t[2] || (t[2] = y((i) => e.tag("###"), ["stop", "prevent"]))
                  }, "Header 3")
                ])
              ]),
              r("li", null, [
                r("h4", null, [
                  r("a", {
                    href: "#!",
                    onClick: t[3] || (t[3] = y((i) => e.tag("####"), ["stop", "prevent"]))
                  }, "Header 4")
                ])
              ]),
              r("li", null, [
                r("h5", null, [
                  r("a", {
                    href: "#!",
                    onClick: t[4] || (t[4] = y((i) => e.tag("#####"), ["stop", "prevent"]))
                  }, "Header 5")
                ])
              ]),
              r("li", null, [
                r("h6", null, [
                  r("a", {
                    href: "#!",
                    onClick: t[5] || (t[5] = y((i) => e.tag("######"), ["stop", "prevent"]))
                  }, "Header 6")
                ])
              ])
            ])
          ]),
          r("li", nt, [
            at,
            r("ul", null, [
              r("li", null, [
                r("strong", null, [
                  r("a", {
                    href: "#!",
                    onClick: t[6] || (t[6] = y((i) => e.symmetricTag("**"), ["stop", "prevent"]))
                  }, "Bold")
                ])
              ]),
              r("li", null, [
                r("em", null, [
                  r("a", {
                    href: "#!",
                    onClick: t[7] || (t[7] = y((i) => e.symmetricTag("*"), ["stop", "prevent"]))
                  }, "Italics")
                ])
              ]),
              r("li", null, [
                r("del", null, [
                  r("a", {
                    href: "#!",
                    onClick: t[8] || (t[8] = y((i) => e.symmetricTag("~~"), ["stop", "prevent"]))
                  }, "Strikethrough")
                ])
              ]),
              r("li", null, [
                r("u", null, [
                  r("a", {
                    href: "#!",
                    onClick: t[9] || (t[9] = y((i) => e.symmetricTag("__"), ["stop", "prevent"]))
                  }, "Underline")
                ])
              ]),
              r("li", null, [
                r("a", {
                  href: "#!",
                  onClick: t[10] || (t[10] = y((i) => e.tag(">"), ["stop", "prevent"]))
                }, "Quote")
              ])
            ])
          ]),
          r("li", null, [
            r("a", {
              href: "#!",
              onClick: t[11] || (t[11] = y((i) => e.tag(`

-------

`), ["stop", "prevent"]))
            }, "HR")
          ]),
          r("li", it, [
            rt,
            r("ul", null, [
              r("li", null, [
                r("a", {
                  href: "#!",
                  onClick: t[12] || (t[12] = y((i) => e.tag("1. "), ["stop", "prevent"])),
                  class: "fa-list-ol"
                }, "Ordered")
              ]),
              r("li", null, [
                r("a", {
                  href: "#!",
                  onClick: t[13] || (t[13] = y((i) => e.tag("* "), ["stop", "prevent"])),
                  class: "fa-list-ul"
                }, "Unordered")
              ])
            ])
          ]),
          r("li", st, [
            ot,
            r("ul", null, [
              r("li", null, [
                r("a", {
                  href: "#!",
                  onClick: t[14] || (t[14] = y((i) => e.showFrame("image"), ["stop", "prevent"])),
                  class: "fa-image"
                })
              ]),
              r("li", null, [
                r("a", {
                  href: "#!",
                  onClick: t[15] || (t[15] = y((i) => e.showFrame("file"), ["stop", "prevent"])),
                  class: "fa-link"
                })
              ])
            ])
          ])
        ])
      ]),
      I(r("textarea", {
        "onUpdate:modelValue": t[16] || (t[16] = (i) => e.internalModel = i),
        id: "editorInput",
        name: "editorInput",
        class: "flex-item",
        onKeyup: t[17] || (t[17] = (...i) => e.keyup && e.keyup(...i))
      }, null, 544), [
        [W, e.internalModel]
      ]),
      lt
    ]),
    e.displayPreview ? (s(), o("div", dt, [
      r("h1", null, h(e.currentTitle), 1),
      r("div", { innerHTML: e.compiledMarkdown }, null, 8, ct)
    ])) : d("", !0),
    e.displayFrame ? (s(), o("div", ut, [
      r("iframe", { src: e.iframeSrc }, null, 8, ht)
    ])) : d("", !0)
  ]);
}
const ft = /* @__PURE__ */ S(We, [["render", mt], ["__scopeId", "data-v-aabc8531"]]), pt = b.defineComponent({
  name: "form-field-select",
  data: function() {
    return {
      internalModel: this.model,
      internalSchema: this.schema,
      optionsQuery: "query($queryType: String!) { dropDown(type: $queryType) { key, value } }",
      errorMessage: ""
    };
  },
  props: {
    model: {
      type: String,
      required: !0,
      default: ""
    },
    schema: {
      type: D,
      required: !0,
      default: () => new D()
    },
    showLabel: {
      type: Boolean,
      required: !1,
      default: !0
    }
  },
  methods: {
    // determines if the field will be validated
    willValidate: function() {
      return this.internalSchema.metadata.required;
    },
    // revalidate the input field
    revalidate: async function() {
      let e = await new Z().validate(document.getElementById(this.getFieldID()));
      return this.errorMessage = e.errorMessage, e.isValid;
    },
    // gets the field id
    getFieldID: function() {
      return this.internalSchema.propertyName.slugify() + this.internalSchema.key;
    },
    // called when the value changes
    // emits the changed event
    // newValue: the new value
    changed: function(e) {
      this.revalidate(), this.$emit("changed", e, this.schema);
    },
    // determines if the value is selected
    // value: the value to check
    isSelected: function(e) {
      return this.internalModel === e.key;
    },
    // gets the values for the select element
    getValues: async function() {
      if (!this.internalSchema.metadata.queryType)
        return;
      let e = this;
      f.debug("Select: Getting values for " + this.internalSchema.metadata.queryType), await w.post("/api/query", {
        query: e.optionsQuery,
        variables: {
          queryType: e.internalSchema.metadata.queryType
        }
      }).onSuccess((t) => {
        f.debug("Select: " + e.internalSchema.metadata.queryType + " loaded successfully.", t.data.dropDown), e.internalSchema.metadata.options = t.data.dropDown;
      }).onError((t) => {
        f.error("Select: " + e.internalSchema.metadata.queryType + " failed to load.", t);
      }).withStorageMode(j.StorageAndUpdate).send();
    }
  },
  // created event, gets the values for the select element
  created: function() {
    this.getValues();
  },
  watch: {
    // watches the model and revalidates when it changes
    // newModel: the new model
    // oldModel: the old model
    model: function(e, t) {
      t !== e && (this.internalModel = e, this.$nextTick(function() {
        this.revalidate();
      }));
    },
    // watches the schema and revalidates when it changes
    // newSchema: the new schema
    // oldSchema: the old schema
    schema: function(e, t) {
      t !== e && (this.internalSchema = e, this.$nextTick(function() {
        this.revalidate();
      }));
    }
  },
  // mounted event, revalidates the field
  mounted: function() {
    this.$nextTick(function() {
      this.revalidate();
    });
  }
}), gt = ["for"], yt = {
  key: 0,
  class: "error clear-background"
}, bt = {
  key: 1,
  class: "clear-background active no-border small"
}, St = /* @__PURE__ */ r("span", { class: "fas fa-info-circle" }, null, -1), $t = {
  key: 2,
  class: "error clear-background"
}, vt = {
  key: 3,
  class: "success clear-background fas fa-check-circle"
}, kt = { key: 1 }, wt = ["disabled", "name", "height", "id", "readonly", "required", "multiple", "width", "data-error-message-value-missing"], Mt = ["value", "selected"];
function Ct(e, t, n, a, l, c) {
  return s(), o("div", null, [
    e.showLabel && e.internalSchema.displayName ? (s(), o("label", {
      key: 0,
      for: e.getFieldID()
    }, [
      p(h(e.internalSchema.displayName) + " ", 1),
      e.internalSchema.metadata.required && e.errorMessage ? (s(), o("span", yt, "*")) : d("", !0),
      e.internalSchema.metadata.hint ? (s(), o("i", bt, [
        St,
        p(h(e.internalSchema.metadata.hint), 1)
      ])) : d("", !0),
      e.errorMessage ? (s(), o("span", $t, h(e.errorMessage), 1)) : d("", !0),
      !e.errorMessage && e.willValidate() ? (s(), o("span", vt)) : d("", !0)
    ], 8, gt)) : d("", !0),
    e.internalSchema.metadata.subtitle ? (s(), o("div", kt, h(e.internalSchema.metadata.subtitle), 1)) : d("", !0),
    I(r("select", {
      "onUpdate:modelValue": t[0] || (t[0] = (i) => e.internalModel = i),
      disabled: e.internalSchema.metadata.disabled,
      name: e.internalSchema.metadata.inputName || e.getFieldID(),
      height: e.internalSchema.metadata.height,
      id: e.getFieldID(),
      onChange: t[1] || (t[1] = (i) => e.changed(e.internalModel)),
      readonly: e.internalSchema.metadata.readonly,
      required: e.internalSchema.metadata.required,
      multiple: e.internalSchema.metadata.multiple,
      class: k(e.internalSchema.metadata.inputClasses),
      width: e.internalSchema.metadata.width,
      "data-error-message-value-missing": e.internalSchema.metadata.errorMessageValueMissing
    }, [
      (s(!0), o($, null, v(e.internalSchema.metadata.options, (i) => (s(), o("option", {
        value: i.key,
        selected: e.isSelected(i),
        key: i.key
      }, h(i.value), 9, Mt))), 128))
    ], 42, wt), [
      [se, e.internalModel]
    ])
  ]);
}
const de = /* @__PURE__ */ S(pt, [["render", Ct]]), Tt = b.defineComponent({
  name: "form-field-checkbox",
  data: function() {
    return {
      internalModel: this.model,
      internalSchema: this.schema,
      errorMessage: ""
    };
  },
  props: {
    model: {
      type: Boolean,
      required: !0,
      default: !1
    },
    schema: {
      type: D,
      required: !0,
      default: () => new D()
    },
    showLabel: {
      type: Boolean,
      required: !1,
      default: !0
    }
  },
  methods: {
    // determines if the field will be validated
    willValidate: function() {
      return this.internalSchema.metadata.required;
    },
    // revalidate the input field
    revalidate: async function() {
      let e = document.getElementById(this.getFieldID());
      if (e == null)
        return;
      let t = await new B().validate(e);
      return this.errorMessage = t.errorMessage, t.isValid;
    },
    // gets the field id
    getFieldID: function() {
      return this.internalSchema.propertyName.slugify() + this.internalSchema.key;
    },
    // called when the value changes
    // newValue: the new value
    // emits the changed event
    changed: function(e) {
      this.revalidate(), this.$emit("changed", e, this.schema);
    }
  },
  watch: {
    // watches the model for changes
    // newModel: the new model
    // oldModel: the old model
    model: function(e, t) {
      t !== e && (this.internalModel = e, this.$nextTick(function() {
        this.revalidate();
      }));
    },
    // watches the schema for changes
    // newSchema: the new schema
    // oldSchema: the old schema
    schema: function(e, t) {
      t !== e && (this.internalSchema = e, this.$nextTick(function() {
        this.revalidate();
      }));
    }
  },
  // mounted event handler, revalidates the field
  mounted: function() {
    this.$nextTick(function() {
      this.revalidate();
    });
  }
}), Et = ["for"], Dt = { key: 0 }, Ft = {
  key: 1,
  class: "error clear-background"
}, It = {
  key: 2,
  class: "error clear-background"
}, qt = {
  key: 3,
  class: "success clear-background fas fa-check-circle"
}, Vt = { key: 4 }, jt = ["id", "checked", "disabled", "dirname", "name", "readonly", "required"];
function At(e, t, n, a, l, c) {
  return s(), o("div", null, [
    r("label", {
      for: e.getFieldID()
    }, [
      e.showLabel && e.internalSchema.displayName ? (s(), o("span", Dt, h(e.internalSchema.displayName), 1)) : d("", !0),
      e.internalSchema.metadata.required && e.errorMessage ? (s(), o("span", Ft, "*")) : d("", !0),
      e.errorMessage ? (s(), o("span", It, h(e.errorMessage), 1)) : d("", !0),
      !e.errorMessage && e.willValidate() ? (s(), o("span", qt)) : d("", !0),
      e.internalSchema.metadata.subtitle ? (s(), o("div", Vt, h(e.internalSchema.metadata.subtitle), 1)) : d("", !0),
      r("input", {
        id: e.getFieldID(),
        type: "checkbox",
        checked: e.internalModel,
        onClick: t[0] || (t[0] = (i) => e.changed(i.target.checked)),
        disabled: e.internalSchema.metadata.disabled,
        dirname: e.internalSchema.metadata.dirname,
        name: e.getFieldID(),
        readonly: e.internalSchema.metadata.readonly,
        required: e.internalSchema.metadata.required
      }, null, 8, jt)
    ], 8, Et)
  ]);
}
const ce = /* @__PURE__ */ S(Tt, [["render", At]]), Ot = b.defineComponent({
  name: "form-field-radio",
  data: function() {
    return {
      internalModel: this.model,
      internalSchema: this.schema,
      optionsQuery: "query($queryType: String!) { dropDown(type: $queryType) { key, value } }",
      errorMessage: ""
    };
  },
  props: {
    model: {
      type: String,
      required: !0,
      default: ""
    },
    schema: {
      type: D,
      required: !0,
      default: () => new D()
    },
    showLabel: {
      type: Boolean,
      required: !1,
      default: !0
    }
  },
  methods: {
    // determines if the field will be validated
    willValidate: function() {
      return this.internalSchema.metadata.required;
    },
    // revalidate the input field
    revalidate: async function() {
      let e = await new Z().validate(document.getElementById(this.getFieldID()));
      return this.errorMessage = e.errorMessage, e.isValid;
    },
    // gets the field id
    getFieldID: function() {
      if (this.internalSchema != null)
        return this.internalSchema.propertyName.slugify() + this.internalSchema.key;
    },
    // get the field name
    getFieldName: function() {
      return this.internalSchema.id ? this.internalSchema.id : this.internalSchema.propertyName.slugify();
    },
    // called when the value changes
    // emits the changed event
    // newValue: the new value
    changed: function(e) {
      this.revalidate(), this.$emit("changed", e, this.schema);
    },
    // Is the item checked?
    // item: item to check
    isItemChecked: function(e) {
      return e === this.internalModel;
    },
    // determines if the value is selected
    // value: the value to check
    isSelected: function(e) {
      return this.internalModel === e.key;
    },
    // gets the values for the select element
    getValues: async function() {
      if (!this.internalSchema.metadata.queryType)
        return;
      let e = this;
      f.debug("Select: Getting values for " + this.internalSchema.metadata.queryType), await w.post("/api/query", {
        query: e.optionsQuery,
        variables: {
          queryType: e.internalSchema.metadata.queryType
        }
      }).onSuccess((t) => {
        f.debug("Select: " + e.internalSchema.metadata.queryType + " loaded successfully.", t.data.dropDown), e.internalSchema.metadata.options = t.data.dropDown;
      }).onError((t) => {
        f.error("Select: " + e.internalSchema.metadata.queryType + " failed to load.", t);
      }).withStorageMode(j.StorageAndUpdate).send();
    }
  },
  // created event, gets the values for the select element
  created: function() {
    this.getValues();
  },
  watch: {
    // watches the model and revalidates when it changes
    // newModel: the new model
    // oldModel: the old model
    model: function(e, t) {
      t !== e && (this.internalModel = e, this.$nextTick(function() {
        this.revalidate();
      }));
    },
    // watches the schema and revalidates when it changes
    // newSchema: the new schema
    // oldSchema: the old schema
    schema: function(e, t) {
      t !== e && (this.internalSchema = e, this.$nextTick(function() {
        this.revalidate();
      }));
    }
  },
  // mounted event, revalidates the field
  mounted: function() {
    this.$nextTick(function() {
      this.revalidate();
    });
  }
}), Lt = ["for"], Nt = {
  key: 0,
  class: "error clear-background"
}, Pt = {
  key: 1,
  class: "clear-background active no-border small"
}, Bt = /* @__PURE__ */ r("span", { class: "fas fa-info-circle" }, null, -1), Rt = {
  key: 2,
  class: "error clear-background"
}, Ut = {
  key: 3,
  class: "success clear-background fas fa-check-circle"
}, zt = { key: 1 }, Yt = { class: "flex row text-center" }, Ht = ["id", "checked", "onClick", "disabled", "name", "readonly", "value"];
function Kt(e, t, n, a, l, c) {
  return s(), o("div", null, [
    e.showLabel && e.internalSchema.displayName ? (s(), o("label", {
      key: 0,
      for: e.getFieldID()
    }, [
      p(h(e.internalSchema.displayName) + " ", 1),
      e.internalSchema.metadata.required && e.errorMessage ? (s(), o("span", Nt, "*")) : d("", !0),
      e.internalSchema.metadata.hint ? (s(), o("i", Pt, [
        Bt,
        p(h(e.internalSchema.metadata.hint), 1)
      ])) : d("", !0),
      e.errorMessage ? (s(), o("span", Rt, h(e.errorMessage), 1)) : d("", !0),
      !e.errorMessage && e.willValidate() ? (s(), o("span", Ut)) : d("", !0)
    ], 8, Lt)) : d("", !0),
    e.internalSchema.metadata.subtitle ? (s(), o("div", zt, h(e.internalSchema.metadata.subtitle), 1)) : d("", !0),
    r("div", Yt, [
      (s(!0), o($, null, v(e.internalSchema.metadata.options, (i) => (s(), o("div", {
        key: i.key
      }, [
        r("label", null, [
          r("input", {
            id: e.getFieldID(i.key),
            type: "radio",
            checked: e.isItemChecked(i.key),
            onClick: (u) => e.changed(i.key),
            disabled: e.internalSchema.metadata.disabled,
            name: e.internalSchema.metadata.inputName || e.getFieldName(),
            readonly: e.internalSchema.metadata.readonly,
            class: k(e.internalSchema.metadata.inputClasses),
            value: i.key
          }, null, 10, Ht),
          p(" " + h(e.$filters.capitalize(i.value)), 1)
        ])
      ]))), 128))
    ])
  ]);
}
const ue = /* @__PURE__ */ S(Ot, [["render", Kt]]), Gt = b.defineComponent({
  data: function() {
    return {
      internalModel: this.model,
      errorMessage: ""
    };
  },
  props: {
    model: Object,
    schema: Object,
    idSuffix: String,
    label: {
      default: !0,
      type: Boolean
    }
  },
  computed: {
    charactersRemaining: function() {
      return this.internalModel == null ? 0 : this.schema.maxlength - this.internalModel.length;
    }
  },
  methods: {
    willValidate: function() {
      return this.internalSchema.metadata.required || this.internalSchema.metadata.maxlength || this.internalSchema.metadata.minlength || this.internalSchema.metadata.pattern || this.internalSchema.metadata.min || this.internalSchema.metadata.max;
    },
    revalidate: async function() {
      let e = await new B().validate(document.getElementById(this.getFieldID()));
      return this.errorMessage = e.errorMessage, e.isValid;
    },
    getFieldID: function() {
      let e = "";
      return this.schema.id ? e = this.schema.id : e = this.schema.model.slugify(), this.idSuffix !== void 0 && (e += this.idSuffix), e;
    },
    changed: function(e) {
      this.revalidate(), this.$emit("changed", e, this.schema);
    }
  },
  watch: {
    model: function(e, t) {
      t !== e && (this.internalModel = e, this.$nextTick(function() {
        this.revalidate();
      }));
    }
  },
  mounted: function() {
    this.$nextTick(function() {
      this.revalidate();
    });
  }
}), _t = ["for"], Qt = {
  key: 0,
  class: "error clear-background"
}, Jt = {
  key: 1,
  class: "clear-background active no-border small"
}, Wt = /* @__PURE__ */ r("span", { class: "fas fa-info-circle" }, null, -1), Zt = {
  key: 2,
  class: "error clear-background"
}, Xt = {
  key: 3,
  class: "success clear-background fas fa-check-circle"
}, xt = ["for"], en = {
  key: 0,
  class: "error clear-background"
}, tn = {
  key: 1,
  class: "clear-background active no-border small"
}, nn = /* @__PURE__ */ r("span", { class: "fas fa-info-circle" }, null, -1), an = {
  key: 2,
  class: "error clear-background"
}, rn = {
  key: 3,
  class: "success clear-background fas fa-check-circle"
}, sn = { key: 2 }, on = ["id", "disabled", "height", "maxlength", "minlength", "name", "placeholder", "title", "readonly", "required", "width", "rows", "data-error-message-value-missing", "data-error-message-too-long", "data-error-message-too-short"], ln = {
  key: 3,
  class: "clear-background active right small"
}, dn = /* @__PURE__ */ r("span", { class: "fas fa-info-circle" }, null, -1), cn = /* @__PURE__ */ r("br", { class: "clear" }, null, -1);
function un(e, t, n, a, l, c) {
  return s(), o("div", null, [
    p(" Text Area "),
    !e.schema.label && e.label ? (s(), o("label", {
      key: 0,
      for: e.getFieldID(),
      class: k(e.schema.labelClasses)
    }, [
      p(h(e.$filters.capitalize(e.schema.model)) + " ", 1),
      e.schema.required && e.errorMessage ? (s(), o("span", Qt, "*")) : d("", !0),
      e.schema.hint ? (s(), o("i", Jt, [
        Wt,
        p(h(e.schema.hint), 1)
      ])) : d("", !0),
      e.errorMessage ? (s(), o("span", Zt, h(e.errorMessage), 1)) : d("", !0),
      !e.errorMessage && e.willValidate() ? (s(), o("span", Xt)) : d("", !0)
    ], 10, _t)) : d("", !0),
    e.schema.label && e.label ? (s(), o("label", {
      key: 1,
      for: e.getFieldID(),
      class: k(e.schema.labelClasses)
    }, [
      p(h(e.schema.label) + " ", 1),
      e.schema.required && e.errorMessage ? (s(), o("span", en, "*")) : d("", !0),
      e.schema.hint ? (s(), o("i", tn, [
        nn,
        p(h(e.schema.hint), 1)
      ])) : d("", !0),
      e.errorMessage ? (s(), o("span", an, h(e.errorMessage), 1)) : d("", !0),
      !e.errorMessage && e.willValidate() ? (s(), o("span", rn)) : d("", !0)
    ], 10, xt)) : d("", !0),
    e.internalSchema.metadata.subtitle ? (s(), o("div", sn, h(e.internalSchema.metadata.subtitle), 1)) : d("", !0),
    I(r("textarea", {
      id: e.getFieldID(),
      "onUpdate:modelValue": t[0] || (t[0] = (i) => e.internalModel = i),
      onInput: t[1] || (t[1] = (i) => e.changed(i.target.value)),
      disabled: e.schema.disabled,
      height: e.schema.height,
      maxlength: e.schema.maxlength,
      minlength: e.schema.minlength,
      name: e.schema.inputName || e.getFieldID(),
      placeholder: e.schema.placeholder,
      title: e.schema.placeholder,
      readonly: e.schema.readonly,
      class: k(e.schema.inputClasses),
      required: e.schema.required,
      width: e.schema.width,
      rows: e.schema.rows || 3,
      "data-error-message-value-missing": e.schema.errorMessageValueMissing,
      "data-error-message-too-long": e.schema.errorMessageTooLong,
      "data-error-message-too-short": e.schema.errorMessageTooShort
    }, `\r
        `, 42, on), [
      [W, e.internalModel]
    ]),
    e.schema.maxlength ? (s(), o("div", ln, [
      dn,
      p(" " + h(e.charactersRemaining) + " characters remaining (" + h(e.schema.maxlength) + " max) ", 1)
    ])) : d("", !0),
    cn
  ]);
}
const he = /* @__PURE__ */ S(Gt, [["render", un]]), hn = b.defineComponent({
  data: function() {
    return {
      internalModel: this.model
    };
  },
  props: {
    model: Object,
    schema: Object,
    idSuffix: String
  },
  methods: {},
  watch: {
    model: function(e, t) {
      t !== e && (this.internalModel = e);
    }
  }
}), mn = ["innerHTML"];
function fn(e, t, n, a, l, c) {
  return s(), o("div", null, [
    p(" Text "),
    r("div", {
      class: k(e.schema.classes),
      innerHTML: e.internalModel || e.schema.model
    }, null, 10, mn)
  ]);
}
const me = /* @__PURE__ */ S(hn, [["render", fn]]), pn = b.defineComponent({
  data() {
    return {
      files: [],
      ready: !1,
      filesAdded: 0,
      internalModel: this.model,
      errorMessage: ""
    };
  },
  props: {
    model: Object,
    schema: Object,
    idSuffix: String
  },
  methods: {
    willValidate: function() {
      return this.internalSchema.metadata.required || this.internalSchema.metadata.maxlength || this.internalSchema.metadata.minlength || this.internalSchema.metadata.pattern || this.internalSchema.metadata.min || this.internalSchema.metadata.max;
    },
    revalidate: async function() {
      let e = await new B().validate(document.getElementById(this.getFieldID()));
      return this.errorMessage = e.errorMessage, e.isValid;
    },
    getFieldID: function() {
      let e = "";
      return this.schema.id ? e = this.schema.id : e = this.schema.model.slugify(), this.idSuffix !== void 0 && (e += this.idSuffix), e;
    },
    changed: function(e) {
      this.revalidate();
      let t = this;
      t.filesAdded = t.filesAdded + e.target.files.length, t.ready = !1;
      for (let n = 0; n < e.target.files.length; ++n) {
        let a = new FileReader();
        a.onload = function(l) {
          return function(c) {
            t.files = t.files.concat({ filename: l, data: t.base64ArrayBuffer(a.result) }), t.files.length === t.filesAdded && (t.ready = !0);
          };
        }(e.target.files[n].name), a.readAsArrayBuffer(e.target.files[n]);
      }
      this.check();
    },
    base64ArrayBuffer: function(e) {
      let t = "", n = new Uint8Array(e), a = n.byteLength;
      for (let l = 0; l < a; ++l)
        t += String.fromCharCode(n[l]);
      return window.btoa(t);
    },
    check: function() {
      if (this.ready === !0) {
        this.$emit("changed", this.files, this.schema);
        return;
      }
      setTimeout(this.check, 100);
    },
    removeFile: function(e) {
      let t = this.files.indexOf(e);
      this.files.splice(t, 1), this.filesAdded--;
    },
    generateGuid: function(e) {
      let t = e.key;
      if (t)
        return t;
      let n = "";
      for (let a = 0; a < 32; a++) {
        let l = Math.floor(Math.random() * 16).toString(16).toUpperCase();
        n = n + l;
      }
      return e.key = n, e.key;
    }
  },
  watch: {
    model: function(e, t) {
      t !== e && (this.internalModel = e, this.$nextTick(function() {
        this.revalidate();
      }));
    }
  },
  mounted: function() {
    this.$nextTick(function() {
      this.revalidate();
    });
  }
}), gn = ["for"], yn = {
  key: 0,
  class: "error clear-background"
}, bn = {
  key: 1,
  class: "clear-background active no-border small"
}, Sn = /* @__PURE__ */ r("span", { class: "fas fa-info-circle" }, null, -1), $n = {
  key: 2,
  class: "error clear-background"
}, vn = {
  key: 3,
  class: "success clear-background fas fa-check-circle"
}, kn = ["for"], wn = {
  key: 0,
  class: "error clear-background"
}, Mn = {
  key: 1,
  class: "clear-background active no-border small"
}, Cn = /* @__PURE__ */ r("span", { class: "fas fa-info-circle" }, null, -1), Tn = {
  key: 2,
  class: "error clear-background"
}, En = {
  key: 3,
  class: "success clear-background fas fa-check-circle"
}, Dn = { key: 2 }, Fn = ["accept", "id", "multiple", "name", "required", "data-error-message-value-missing"], In = {
  key: 3,
  class: "flex"
}, qn = ["onClick"], Vn = { class: "body" };
function jn(e, t, n, a, l, c) {
  return s(), o("div", null, [
    p(" Upload "),
    e.schema.label ? d("", !0) : (s(), o("label", {
      key: 0,
      for: e.getFieldID(),
      class: k(e.schema.labelClasses)
    }, [
      p(h(e.$filters.capitalize(e.schema.model)) + " ", 1),
      e.schema.required && e.errorMessage ? (s(), o("span", yn, "*")) : d("", !0),
      e.schema.hint ? (s(), o("i", bn, [
        Sn,
        p(h(e.schema.hint), 1)
      ])) : d("", !0),
      e.errorMessage ? (s(), o("span", $n, h(e.errorMessage), 1)) : d("", !0),
      !e.errorMessage && e.willValidate() ? (s(), o("span", vn)) : d("", !0)
    ], 10, gn)),
    e.schema.label ? (s(), o("label", {
      key: 1,
      for: e.getFieldID(),
      class: k(e.schema.labelClasses)
    }, [
      p(h(e.schema.label) + " ", 1),
      e.schema.required && e.errorMessage ? (s(), o("span", wn, "*")) : d("", !0),
      e.schema.hint ? (s(), o("i", Mn, [
        Cn,
        p(h(e.schema.hint), 1)
      ])) : d("", !0),
      e.errorMessage ? (s(), o("span", Tn, h(e.errorMessage), 1)) : d("", !0),
      !e.errorMessage && e.willValidate() ? (s(), o("span", En)) : d("", !0)
    ], 10, kn)) : d("", !0),
    e.internalSchema.metadata.subtitle ? (s(), o("div", Dn, h(e.internalSchema.metadata.subtitle), 1)) : d("", !0),
    r("div", {
      class: k(["file-upload", e.schema.inputClasses])
    }, [
      p(h(e.schema.placeholder) + " ", 1),
      r("input", {
        accept: e.schema.accept,
        id: e.getFieldID(),
        onChange: t[0] || (t[0] = (i) => e.changed(i)),
        multiple: e.schema.multiple,
        name: e.schema.inputName || e.getFieldID(),
        required: e.schema.required,
        type: "file",
        "data-error-message-value-missing": e.schema.errorMessageValueMissing
      }, null, 40, Fn)
    ], 2),
    e.schema.uploadPreview ? (s(), o("div", In, [
      (s(!0), o($, null, v(e.files, (i) => (s(), o("div", {
        class: k(["upload-preview panel", e.schema.previewClasses]),
        key: e.generateGuid(i)
      }, [
        r("header", null, [
          r("div", {
            class: "header",
            onClick: (u) => e.removeFile(i)
          }, "×", 8, qn),
          p(" ")
        ]),
        r("div", Vn, h(i.filename), 1)
      ], 2))), 128))
    ])) : d("", !0)
  ]);
}
const fe = /* @__PURE__ */ S(pn, [["render", jn]]), An = b.defineComponent({
  data: function() {
    return {
      internalModel: this.model
    };
  },
  props: {
    model: Object,
    schema: Object,
    idSuffix: String
  },
  methods: {
    click: function(e, t) {
      return this.$emit("click", e, t), !1;
    },
    generateGuid: function(e) {
      let t = e.key;
      if (t)
        return t;
      let n = "";
      for (let a = 0; a < 32; a++) {
        let l = Math.floor(Math.random() * 16).toString(16).toUpperCase();
        n = n + l;
      }
      return e.key = n, e.key;
    }
  },
  watch: {
    model: function(e, t) {
      t !== e && (this.internalModel = e);
    }
  }
}), On = { class: "controls" }, Ln = { class: "input-group" }, Nn = ["type", "value", "onClick"];
function Pn(e, t, n, a, l, c) {
  return s(), o("div", On, [
    p(" Buttons "),
    r("div", Ln, [
      (s(!0), o($, null, v(e.schema.buttons, (i) => (s(), o("input", {
        key: e.generateGuid(i),
        type: i.type,
        value: i.value,
        class: k(i.classes),
        onClick: (u) => e.click(u, i)
      }, null, 10, Nn))), 128))
    ])
  ]);
}
const pe = /* @__PURE__ */ S(An, [["render", Pn]]), Bn = b.defineComponent({
  data: function() {
    return {
      internalModel: this.model
    };
  },
  props: {
    model: Object,
    schema: Object,
    idSuffix: String
  },
  methods: {
    show: function(e) {
      return typeof e == "function" ? e() : e;
    },
    getFieldType: function(e) {
      return e.type.indexOf("form-field-") == 0 ? e.type : "form-field-" + e.type;
    },
    getModelValue: function(e) {
      return this.internalModel[e.model];
    },
    error: function(e) {
      this.$emit("error", e);
    },
    exception: function(e) {
      this.$emit("exception", e);
    },
    setModelValue: function(e, t) {
      this.internalModel[t.model] = e, this.$emit("changed", this.internalModel, this.schema);
    },
    buttonClicked: function(e, t) {
      this.$emit("click", e, t);
    },
    getSchema: function(e) {
      return e.type.startsWith("complex") ? (e.schema.model === void 0 && (e.schema.model = e.model), e.schema) : e;
    },
    getIDSuffix: function(e) {
      return this.idSuffix === void 0 ? "" : this.idSuffix;
    },
    generateGuid: function(e) {
      let t = e.key;
      if (t)
        return t;
      let n = "";
      for (let a = 0; a < 32; a++) {
        let l = Math.floor(Math.random() * 16).toString(16).toUpperCase();
        n = n + l;
      }
      return e.key = n, e.key;
    }
  },
  watch: {
    model: function(e, t) {
      t !== e && (this.internalModel = e);
    }
  }
}), Rn = { key: 0 };
function Un(e, t, n, a, l, c) {
  return s(), o("div", null, [
    p(" Conditional "),
    e.show(e.internalModel.display || e.schema.display) ? (s(), o("div", Rn, [
      (s(!0), o($, null, v(e.schema.fields, (i) => (s(), o("div", {
        key: e.generateGuid(i)
      }, [
        (s(), q(V(e.getFieldType(i)), {
          schema: e.getSchema(i),
          model: e.getModelValue(i),
          "id-suffix": e.getIDSuffix(i),
          onChanged: e.setModelValue,
          onClick: e.buttonClicked,
          onError: e.error,
          onException: e.exception
        }, null, 40, ["schema", "model", "id-suffix", "onChanged", "onClick", "onError", "onException"]))
      ]))), 128))
    ])) : d("", !0)
  ]);
}
const zn = /* @__PURE__ */ S(Bn, [["render", Un]]), Yn = b.defineComponent({
  name: "form-field-complex-list",
  components: {
    "form-field-input": le,
    "form-field-select": de,
    "form-field-checkbox": ce,
    "form-field-radio": ue,
    "form-field-text-area": he,
    "form-field-text": me,
    "form-field-upload": fe,
    "form-field-buttons": pe
  },
  data: function() {
    return {
      internalModel: this.model,
      internalSchema: this.schema,
      defaultItem: this.getDefaultValue(this.schema)
    };
  },
  props: {
    model: {
      type: Object,
      default: {}
    },
    schema: {
      type: Object,
      default: {
        fields: []
      }
    }
  },
  methods: {
    // Gets the default value for an item in the list based on the schema
    // schema: The schema to get the default value for
    getDefaultValue(e) {
      if (e == null)
        return {};
      let t = {};
      for (let n = 0; n < e.metadata.fields.length; ++n) {
        let a = e.metadata.fields[n];
        if (a.propertyType.indexOf("complex") == -1)
          if (a.metadata.inputType === "date" || a.metadata.inputType === "datetime-local" || a.metadata.inputType === "datetime" || a.metadata.inputType === "month") {
            let l = moment(/* @__PURE__ */ new Date());
            a.isUTC && (l = moment.utc(/* @__PURE__ */ new Date()).local()), a.inputType === "date" ? t[a.propertyName] = l.format("YYYY-MM-DD") : a.inputType === "datetime-local" || a.inputType === "datetime" ? t[a.propertyName] = l.format("YYYY-MM-DDTHH:mm") : a.inputType === "month" && (t[a.propertyName] = l.format("YYYY-MM"));
          } else
            t[a.propertyName] = "";
        else
          a.propertyType.indexOf("complex-list") > -1 || a.propertyType.indexOf("complex-repeater") > -1 ? t[a.propertyName] = [] : t[a.propertyName] = this.getDefaultValue(a);
      }
      return t;
    },
    // Gets the field type for a given field
    // field: The field to get the type for
    getFieldType: function(e) {
      return "form-field-" + e.propertyType;
    },
    // Gets the model value for a given field and item
    // field: The field to get the value for
    // item: The item to get the value for
    getModelValue: function(e, t) {
      return t[e.propertyName];
    },
    // Sets the model value for a given field and item
    // Emits the changed event
    // field: The field to set the value for
    // item: The item to set the value for
    // newValue: The new value to set
    setModelValue: function(e, t, n) {
      t[e.propertyName] = n, this.$emit("changed", this.internalModel, this.schema);
    },
    // Emits the error event
    error: function(e) {
      this.$emit("error", e);
    },
    // Emits the exception event
    exception: function(e) {
      this.$emit("exception", e);
    },
    // Emits the click event when a button is clicked
    buttonClicked: function(e, t) {
      this.$emit("click", e, t);
    },
    // Removes an item from the list
    // item: The item to remove
    removeItem: function(e) {
      if (this.internalSchema.confirmRemoval && !confirm("Are you sure you want to remove this item?"))
        return;
      this.internalModel || (this.internalModel = []);
      let t = this.internalModel.indexOf(e);
      this.internalModel.splice(t, 1), this.$emit("changed", this.internalModel, this.internalSchema);
    },
    // Adds an item to the list using the default value
    // Emits the changed event
    addItem: function() {
      this.internalModel || (this.internalModel = []), this.internalModel = this.internalModel.concat(Object.assign({}, this.defaultItem)), this.$emit("changed", this.internalModel, this.schema);
    },
    // Generates a guid for an item
    generateGuid: function(e) {
      let t = e.key;
      if (t)
        return t;
      let n = "";
      for (let a = 0; a < 32; a++) {
        let l = Math.floor(Math.random() * 16).toString(16).toUpperCase();
        n = n + l;
      }
      return e.key = n, e.key;
    }
  },
  watch: {
    // Watches for changes to the model and updates the internal model
    // newModel: The new model value
    // oldModel: The old model value
    model: function(e, t) {
      t !== e && (this.internalModel = e);
    },
    // Watches for changes to the schema and updates the internal schema
    // newSchema: The new schema value
    // oldSchema: The old schema value
    schema: function(e, t) {
      t !== e && (this.internalSchema = e);
    }
  }
}), Hn = { key: 0 }, Kn = { class: "form-table" }, Gn = ["data-tooltip"], _n = /* @__PURE__ */ r("span", { class: "fas fa-info-circle no-border small" }, null, -1), Qn = [
  _n
], Jn = /* @__PURE__ */ r("th", null, null, -1), Wn = ["onClick"], Zn = ["colspan"], Xn = /* @__PURE__ */ r("span", { class: "fas fa-plus-circle" }, null, -1);
function xn(e, t, n, a, l, c) {
  return s(), o("div", null, [
    e.internalSchema.displayName ? (s(), o("h2", Hn, h(e.$filters.capitalize(e.internalSchema.displayName)), 1)) : d("", !0),
    r("table", Kn, [
      r("thead", null, [
        r("tr", null, [
          (s(!0), o($, null, v(e.internalSchema.metadata.fields, (i) => (s(), o("th", {
            key: e.generateGuid(i)
          }, [
            p(h(e.$filters.capitalize(i.displayName)) + " ", 1),
            i.metadata.hint ? (s(), o("span", {
              key: 0,
              "data-tooltip": i.metadata.hint,
              "data-tooltip-size": "extra-large"
            }, Qn, 8, Gn)) : d("", !0)
          ]))), 128)),
          Jn
        ])
      ]),
      r("tbody", null, [
        (s(!0), o($, null, v(e.internalModel, (i, u) => (s(), o("tr", {
          key: e.generateGuid(i)
        }, [
          (s(!0), o($, null, v(e.internalSchema.metadata.fields, (m) => (s(), o("td", {
            key: e.generateGuid(m)
          }, [
            (s(), q(V(e.getFieldType(m)), {
              schema: m,
              model: e.getModelValue(m, i),
              label: !1,
              onChanged: (g) => e.setModelValue(m, i, g),
              onClick: e.buttonClicked,
              onError: e.error,
              onException: e.exception
            }, null, 40, ["schema", "model", "onChanged", "onClick", "onError", "onException"]))
          ]))), 128)),
          r("td", null, [
            r("a", {
              class: "fas fa-minus-circle",
              onClick: y((m) => e.removeItem(i), ["stop", "prevent"])
            }, null, 8, Wn)
          ])
        ]))), 128))
      ]),
      r("tfoot", null, [
        r("tr", null, [
          r("td", {
            colspan: e.internalSchema.metadata.fields.length + 1
          }, [
            r("a", {
              onClick: t[0] || (t[0] = y((...i) => e.addItem && e.addItem(...i), ["stop", "prevent"]))
            }, [
              Xn,
              p("Add More")
            ])
          ], 8, Zn)
        ])
      ])
    ])
  ]);
}
const ea = /* @__PURE__ */ S(Yn, [["render", xn]]), ta = b.defineComponent({
  methods: {
    findTab: function(e) {
      return !this.sections || this.sections.length === 0 ? null : (e || (e = this.sections[0].name), this.sections.filter((t) => t.name === e)[0]);
    },
    switchSelected: function(e) {
      this.sectionPicked = this.findTab(e), this.switchTabs(), this.$emit("section-changed", this.sectionPicked);
    },
    switchTabs: function() {
      if (!(!this.sections || this.sections.length === 0) && (this.sections.some((e) => e === this.sectionPicked) || (this.sectionPicked = this.sections[0]), !!this.sectionPicked)) {
        for (let e = 0; e < this.sections.length; ++e)
          this.sections[e].selected = !1;
        this.sectionPicked.selected = !0;
      }
    }
  },
  props: {
    initialSectionPicked: {
      type: String,
      default: ""
    },
    sections: {
      type: Array,
      default: []
    }
  },
  watch: {
    sections: function(e) {
      var t;
      this.switchSelected((t = this.sectionPicked) == null ? void 0 : t.name);
    },
    initialSectionPicked: function(e) {
      this.switchSelected(this.initialSectionPicked);
    }
  },
  data() {
    return {
      sectionPicked: this.findTab(this.initialSectionPicked)
    };
  },
  beforeMount: function() {
    this.switchSelected(this.initialSectionPicked);
  }
}), na = { class: "tabs" }, aa = { class: "row flex align-items-stretch" }, ia = ["onClick"];
function ra(e, t, n, a, l, c) {
  return s(), o("div", na, [
    r("header", null, [
      r("ul", aa, [
        (s(!0), o($, null, v(e.sections, (i, u) => (s(), o("li", { key: u }, [
          r("a", {
            href: "#!",
            onClick: y((m) => e.switchSelected(i.name), ["stop", "prevent"]),
            class: k(["tab", { selected: i.selected }])
          }, [
            r("span", {
              class: k(["fas", [i.icon]])
            }, null, 2),
            p(" " + h(i.name), 1)
          ], 10, ia)
        ]))), 128))
      ])
    ]),
    r("section", null, [
      Ce(e.$slots, "default")
    ])
  ]);
}
const sa = /* @__PURE__ */ S(ta, [["render", ra]]), oa = b.defineComponent({
  components: {
    tabs: sa
  },
  data() {
    return {
      internalModel: this.model,
      tabPicked: this.schema.tabs[0]
    };
  },
  props: {
    model: Object,
    schema: Object,
    idSuffix: String
  },
  methods: {
    getFieldType: function(e) {
      return e.type.indexOf("form-field-") == 0 ? e.type : "form-field-" + e.type;
    },
    getModelValue: function(e) {
      return this.internalModel[this.tabPicked.model][e.model];
    },
    setModelValue: function(e, t) {
      this.internalModel[this.tabPicked.model][t.model] = e, this.$emit("changed", this.internalModel, this.schema);
    },
    error: function(e) {
      this.$emit("error", e);
    },
    exception: function(e) {
      this.$emit("exception", e);
    },
    buttonClicked: function(e, t) {
      this.$emit("click", e, t);
    },
    getSchema: function(e) {
      return e;
    },
    tabChanged: function(e) {
      this.tabPicked = e;
    },
    getFields: function() {
      return this.tabPicked.fields;
    },
    getIDSuffix: function(e) {
      return this.idSuffix === void 0 ? "" : this.idSuffix;
    },
    generateGuid: function(e) {
      let t = e.key;
      if (t)
        return t;
      let n = "";
      for (let a = 0; a < 32; a++) {
        let l = Math.floor(Math.random() * 16).toString(16).toUpperCase();
        n = n + l;
      }
      return e.key = n, e.key;
    }
  },
  watch: {
    model: function(e, t) {
      t !== e && (this.internalModel = e);
    }
  }
});
function la(e, t, n, a, l, c) {
  const i = O("tabs");
  return s(), o("div", null, [
    p(" Complex Tabs "),
    L(i, {
      sections: e.schema.tabs,
      onSectionChanged: e.tabChanged,
      class: k(e.schema.tabClasses)
    }, {
      default: Te(() => [
        (s(!0), o($, null, v(e.getFields(), (u) => (s(), o("div", {
          key: e.generateGuid(u)
        }, [
          (s(), q(V(e.getFieldType(u)), {
            schema: e.getSchema(u),
            model: e.getModelValue(u),
            "id-suffix": e.getIDSuffix(u),
            onChanged: e.setModelValue,
            onClick: e.buttonClicked,
            onError: e.error,
            onException: e.exception
          }, null, 40, ["schema", "model", "id-suffix", "onChanged", "onClick", "onError", "onException"]))
        ]))), 128))
      ]),
      _: 1
    }, 8, ["sections", "onSectionChanged", "class"])
  ]);
}
const da = /* @__PURE__ */ S(oa, [["render", la]]), ca = b.defineComponent({
  data: function() {
    let e = {};
    for (let t in this.model[0])
      this.model[0].hasOwnProperty(t) && (e[t] = "");
    return {
      internalModel: this.model,
      defaultItem: e
    };
  },
  props: {
    model: Object,
    schema: Object,
    idSuffix: String
  },
  methods: {
    getFieldType: function(e) {
      return e.type.indexOf("form-field-") == 0 ? e.type : "form-field-" + e.type;
    },
    getModelValue: function(e, t) {
      return t[e.model];
    },
    setModelValue: function(e, t, n) {
      t[e.model] = n, this.$emit("changed", this.internalModel, this.schema);
    },
    error: function(e) {
      this.$emit("error", e);
    },
    exception: function(e) {
      this.$emit("exception", e);
    },
    buttonClicked: function(e, t) {
      this.$emit("click", e, t);
    },
    getSchema: function(e) {
      return e;
    },
    removeItem: function(e) {
      if (this.schema.confirmRemoval && !confirm("Are you sure you want to remove this item?"))
        return;
      this.internalModel || (this.internalModel = []);
      let t = this.internalModel.indexOf(e);
      this.internalModel.splice(t, 1), this.$emit("changed", this.internalModel, this.schema);
    },
    addItem: function(e) {
      this.internalModel || (this.internalModel = []), this.internalModel = this.internalModel.concat(Object.assign({}, this.defaultItem)), this.$emit("changed", this.internalModel, this.schema);
    },
    getIDSuffix: function(e, t) {
      return this.idSuffix === void 0 ? t : this.idSuffix + t;
    },
    generateGuid: function(e) {
      let t = e.key;
      if (t)
        return t;
      let n = "";
      for (let a = 0; a < 32; a++) {
        let l = Math.floor(Math.random() * 16).toString(16).toUpperCase();
        n = n + l;
      }
      return e.key = n, e.key;
    }
  },
  watch: {
    model: function(e, t) {
      t !== e && (this.internalModel = e);
    }
  }
}), ua = ["onClick"], ha = /* @__PURE__ */ r("span", { class: "fas fa-plus-circle" }, null, -1);
function ma(e, t, n, a, l, c) {
  return s(), o("div", null, [
    p(" Complex Repeater "),
    (s(!0), o($, null, v(e.internalModel, (i, u) => (s(), o("div", {
      key: e.generateGuid(i),
      class: "border-bottom"
    }, [
      r("a", {
        class: "fas fa-minus-circle right",
        onClick: y((m) => e.removeItem(i), ["stop", "prevent"])
      }, null, 8, ua),
      (s(!0), o($, null, v(e.schema.fields, (m) => (s(), o("div", {
        key: e.generateGuid(m)
      }, [
        (s(), q(V(e.getFieldType(m)), {
          schema: e.getSchema(m),
          model: e.getModelValue(m, i),
          label: !0,
          "id-suffix": e.getIDSuffix(m, u),
          onChanged: (g) => e.setModelValue(m, i, g),
          onClick: e.buttonClicked,
          onError: e.error,
          onException: e.exception
        }, null, 40, ["schema", "model", "id-suffix", "onChanged", "onClick", "onError", "onException"]))
      ]))), 128))
    ]))), 128)),
    r("a", {
      onClick: t[0] || (t[0] = y((...i) => e.addItem && e.addItem(...i), ["stop", "prevent"]))
    }, [
      ha,
      p("Add More")
    ])
  ]);
}
const fa = /* @__PURE__ */ S(ca, [["render", ma]]), pa = b.defineComponent({
  name: "form-field-complex",
  components: {
    "form-field-complex-conditional": zn,
    "form-field-complex-list": ea,
    "form-field-complex-tabs": da,
    "form-field-input": le,
    "form-field-select": de,
    "form-field-checkbox": ce,
    "form-field-radio": ue,
    "form-field-text-area": he,
    "form-field-text": me,
    "form-field-upload": fe,
    "form-field-buttons": pe,
    "form-field-complex-repeater": fa,
    "form-field-html": ft
  },
  data: function() {
    return {
      internalModel: this.model,
      internalSchema: this.schema
    };
  },
  props: {
    model: {
      type: Object,
      default: {}
    },
    schema: {
      type: Object,
      default: {
        fields: []
      }
    }
  },
  methods: {
    // When an error occurs, emit the error event
    // errorCode: contains the error code
    error: function(e) {
      this.$emit("error", e);
    },
    // When an exception occurs, emit the exception event
    // errorCode: contains the error code
    exception: function(e) {
      this.$emit("exception", e);
    },
    // When a field is changed, emit the changed event
    // newValue: contains the new value of the field
    // field: contains the field object
    setModelValue: function(e, t) {
      this.internalModel[t.propertyName] = e, this.$emit("changed", this.internalModel, this.schema);
    },
    // button clicked event
    // event: contains the event object
    // field: contains the field object
    buttonClicked: function(e, t) {
      this.$emit("click", e, t);
    },
    // generate a guid
    // item: contains the object
    generateGuid: function(e) {
      let t = e.key;
      if (t)
        return t;
      let n = "";
      for (let a = 0; a < 32; a++) {
        let l = Math.floor(Math.random() * 16).toString(16).toUpperCase();
        n = n + l;
      }
      return e.key = n, e.key;
    }
  },
  watch: {
    // watch for changes in the model
    // newModel: contains the new model
    // oldModel: contains the old model
    model: function(e, t) {
      t !== e && (this.internalModel = e);
    },
    // watch for changes in the schema
    // newSchema: contains the new schema
    // oldSchema: contains the old schema
    schema: function(e, t) {
      t !== e && (this.internalSchema = e);
    }
  }
});
function ga(e, t, n, a, l, c) {
  return s(), o("div", null, [
    e.internalModel ? (s(!0), o($, { key: 0 }, v(e.internalSchema.fields, (i) => (s(), o("div", {
      key: e.generateGuid(i)
    }, [
      (s(), q(V("form-field-" + i.propertyType), {
        schema: i,
        model: e.internalModel[i.propertyName],
        onChanged: e.setModelValue,
        onClick: e.buttonClicked,
        onError: e.error,
        onException: e.exception
      }, null, 40, ["schema", "model", "onChanged", "onClick", "onError", "onException"]))
    ]))), 128)) : d("", !0)
  ]);
}
const ya = /* @__PURE__ */ S(pa, [["render", ga]]), ba = b.defineComponent({
  name: "mithril-form",
  components: {
    "form-field-complex": ya
  },
  data: function() {
    return {
      submitting: !1,
      internalModel: JSON.parse(JSON.stringify(this.model)),
      internalSchema: { fields: this.schema },
      formID: "mithril-form-" + Math.floor(Math.random() * 1e9),
      valid: !1
    };
  },
  props: {
    schema: {
      type: Array,
      default: []
    },
    model: {
      type: Object,
      default: {}
    },
    action: {
      default: "",
      type: String
    },
    ajaxAction: {
      default: "",
      type: String
    },
    encoding: {
      default: "multipart/form-data",
      type: String
    },
    debug: {
      default: !0,
      type: Boolean
    }
  },
  methods: {
    // revalidate the input field
    revalidate: async function() {
      return this.valid = await Le.validateForm(document.getElementById(this.formID)), this.valid;
    },
    // set the model value and revalidate
    // newValue: the new value to set
    // field: the field to set
    setModelValue: function(e, t) {
      this.internalModel = e, this.revalidate(), this.$emit("changed", this.internalModel);
    },
    // emit an error event
    // errorCode: the error code to emit
    error: function(e) {
      this.$emit("error", e);
    },
    // emit an exception event
    // errorCode: the error code to emit
    exception: function(e) {
      this.$emit("exception", e);
    },
    // emit a click event on a button click
    // event: the event that was clicked
    // field: the field that was clicked
    buttonClicked: function(e, t) {
      this.revalidate(), this.$emit("click", e, t);
    },
    // reset the form to the original state and revalidate
    reset: function() {
      let e = this;
      f.debug("Resetting form to state:", e.model), e.internalModel = JSON.parse(JSON.stringify(e.model)), setTimeout(function() {
        e.revalidate();
      }, 100);
    },
    // submit the form to the server
    // event: the event that was submitted
    submit: function(e) {
      let t = this;
      return !t.revalidate() || t.submitting ? (e.preventDefault(), !1) : (t.submitting = !0, t.ajaxAction ? (f.debug("Submitting form to location: ", { location: t.ajaxAction }), w.post(t.ajaxAction, t.internalModel).onSuccess(function(n) {
        t.submitting = !1, t.$emit("success", n);
      }).onError(function(n) {
        t.submitting = !1, t.$emit("error", n);
      }).send(), e.preventDefault(), !1) : (this.$emit("submit", e, t.internalModel), e.preventDefault(), !1));
    },
    // get the ID suffix for the form
    getIDSuffix: function() {
      return "";
    }
  },
  watch: {
    // watch for changes to the schema
    // newSchema: the new schema
    // oldSchema: the old schema
    schema: function(e, t) {
      t !== e && (this.internalSchema = e, this.internalModel !== null && this.$nextTick(() => {
        this.revalidate();
      }));
    },
    // watch for changes to the model
    // newModel: the new model
    // oldModel: the old model
    model: function(e, t) {
      t !== e && (this.internalModel = e, e !== null && this.$nextTick(() => {
        this.revalidate();
      }));
    }
  },
  // mounted event handler for the component (revalidate the form)
  mounted: function() {
    this.$nextTick(function() {
      this.revalidate();
    });
  }
});
const Y = (e) => (N("data-v-c5bd6270"), e = e(), P(), e), Sa = ["action", "enctype", "id"], $a = {
  key: 0,
  class: "panel error"
}, va = /* @__PURE__ */ Y(() => /* @__PURE__ */ r("header", null, "Oops, some form elements have problems", -1)), ka = /* @__PURE__ */ Y(() => /* @__PURE__ */ r("div", { class: "body" }, " We're sorry but some of the fields are having issues. ", -1)), wa = [
  va,
  ka
], Ma = { class: "input-group" }, Ca = ["disabled", "value"], Ta = ["disabled"], Ea = {
  key: 1,
  class: "flex row no-wrap"
}, Da = { class: "panel" }, Fa = /* @__PURE__ */ Y(() => /* @__PURE__ */ r("header", null, "Form Schema", -1)), Ia = { class: "body" }, qa = { class: "panel" }, Va = /* @__PURE__ */ Y(() => /* @__PURE__ */ r("header", null, "Form Model", -1)), ja = { class: "body" };
function Aa(e, t, n, a, l, c) {
  const i = O("form-field-complex");
  return s(), o("form", {
    action: e.action,
    class: "stacked",
    onReset: t[0] || (t[0] = y((...u) => e.reset && e.reset(...u), ["stop", "prevent"])),
    onSubmit: t[1] || (t[1] = (...u) => e.submit && e.submit(...u)),
    method: "post",
    enctype: e.encoding,
    id: e.formID
  }, [
    e.valid ? d("", !0) : (s(), o("div", $a, wa)),
    L(i, {
      schema: e.internalSchema,
      model: e.internalModel,
      onChanged: e.setModelValue,
      onClick: e.buttonClicked,
      onError: e.error,
      onException: e.exception
    }, null, 8, ["schema", "model", "onChanged", "onClick", "onError", "onException"]),
    r("div", null, [
      r("div", Ma, [
        r("input", {
          type: "submit",
          disabled: e.submitting,
          value: e.submitting ? "Submitting..." : "Submit"
        }, null, 8, Ca),
        r("input", {
          type: "reset",
          disabled: e.submitting,
          value: "Reset"
        }, null, 8, Ta)
      ])
    ]),
    e.debug ? (s(), o("div", Ea, [
      r("div", Da, [
        Fa,
        r("div", Ia, [
          r("pre", null, "                        " + h(e.internalSchema) + `
                    `, 1)
        ])
      ]),
      r("div", qa, [
        Va,
        r("div", ja, [
          r("pre", null, "                        " + h(e.internalModel) + `
                    `, 1)
        ])
      ])
    ])) : d("", !0)
  ], 40, Sa);
}
const ge = /* @__PURE__ */ S(ba, [["render", Aa], ["__scopeId", "data-v-c5bd6270"]]), Oa = b.defineComponent({
  name: "settings-editor-component",
  components: {
    "mithril-form": ge
  },
  data: function() {
    return {
      currentEntity: null,
      entityQuery: `query($entityType: String) {
                    entity(entityType: $entityType)
                }`
    };
  },
  methods: {
    // close the settings editor
    close: function() {
      this.$emit("close");
    },
    // load the entity
    loadData: z(async function() {
      let e = this;
      await w.post("/api/query", {
        query: e.entityQuery,
        variables: {
          entityType: e.schema.dataType
        }
      }).onSuccess((n) => {
        f.debug("Entity loaded successfully", n.data.entity), e.currentEntity = n.data.entity;
      }).send();
    }, 100),
    // save the entity
    // event: the event that triggered the save
    // entity: the entity to save
    saveEntity: function(e, t) {
      let n = this;
      if (e.preventDefault(), t != null)
        return this.currentEntity = null, f.debug(t), w.post("/api/command/v1/SaveModelCommand", {
          data: t,
          entityType: n.schema.dataType,
          id: t.iD
        }).onSuccess((a) => {
          f.debug("Entity saved successfully", a.data), n.close();
        }).onError((a) => {
          f.error("Error saving entity", a.data);
        }).send(), !1;
    }
  },
  props: {
    name: {
      type: String,
      default: ""
    },
    schema: {
      type: Object,
      default: {}
    },
    debug: {
      type: Boolean,
      default: !0
    }
  },
  // created event handler (load the entity)
  created: function() {
    this.loadData();
  },
  watch: {
    schema: function() {
      this.loadData();
    }
  }
}), La = { class: "body" }, Na = {
  key: 0,
  class: "panel debug"
}, Pa = /* @__PURE__ */ r("header", null, "Editor Schema", -1);
function Ba(e, t, n, a, l, c) {
  const i = O("mithril-form");
  return s(), o("div", {
    class: k(["panel", e.schema.class])
  }, [
    r("header", null, h(e.name), 1),
    r("div", La, [
      r("div", null, [
        L(i, {
          schema: e.schema.modelSchema,
          model: e.currentEntity,
          debug: e.debug,
          onSubmit: e.saveEntity
        }, null, 8, ["schema", "model", "debug", "onSubmit"])
      ]),
      e.debug && e.schema ? (s(), o("div", Na, [
        Pa,
        r("pre", null, "" + h(e.schema) + `
            `, 1)
      ])) : d("", !0)
    ])
  ], 2);
}
const Ra = /* @__PURE__ */ S(Oa, [["render", Ba]]);
class ye {
  // Constructor
  constructor(t, n, a, l, c) {
    this.filter = t, this.pageSize = n, this.page = a, this.sortField = l, this.sortAscending = c;
  }
}
const Ua = b.defineComponent({
  name: "mithril-listing",
  data: function() {
    return {
      internalModel: this.model,
      internalSchema: this.schema.filter((e) => {
        var t;
        return ((t = e.metadata) == null ? void 0 : t.canList) ?? !0;
      }),
      filter: "",
      pageSize: 10,
      page: 0,
      sortField: "",
      sortAscending: !1
    };
  },
  props: {
    model: {
      type: Object,
      default: {}
    },
    schema: {
      type: Array,
      default: []
    },
    debug: {
      type: Boolean,
      default: !1
    }
  },
  methods: {
    // filter data event is being raised
    filterData: z(function(e) {
      let t = this;
      t.$emit("filter", e, new ye(t.filter, t.pageSize, t.page, t.sortField, t.sortAscending));
    }, 300),
    // entity selected event is being raised
    // entity: the entity that was selected
    entitySelected: function(e) {
      this.$emit("entity-selected", e);
    }
  },
  watch: {
    // model changed
    // newModel: the new model
    // oldModel: the old model
    model: function(e, t) {
      t !== e && (this.internalModel = e);
    },
    // schema changed
    // newSchema: the new schema
    // oldSchema: the old schema
    schema: function(e, t) {
      t !== e && (this.internalSchema = e.filter((n) => {
        var a;
        return ((a = n.metadata) == null ? void 0 : a.canList) ?? !0;
      }));
    }
  }
});
const x = (e) => (N("data-v-f557b045"), e = e(), P(), e), za = { class: "flex row" }, Ya = { class: "right" }, Ha = /* @__PURE__ */ Ee('<option value="10" data-v-f557b045>10</option><option value="25" data-v-f557b045>25</option><option value="50" data-v-f557b045>50</option><option value="100" data-v-f557b045>100</option><option value="-1" data-v-f557b045>All</option>', 5), Ka = [
  Ha
], Ga = /* @__PURE__ */ x(() => /* @__PURE__ */ r("br", { class: "clear" }, null, -1)), _a = ["onClick"], Qa = {
  key: 0,
  class: "flex row"
}, Ja = { class: "panel" }, Wa = /* @__PURE__ */ x(() => /* @__PURE__ */ r("header", null, "Listing Schema", -1)), Za = { class: "body" }, Xa = { class: "panel" }, xa = /* @__PURE__ */ x(() => /* @__PURE__ */ r("header", null, "Listing Model", -1)), ei = { class: "body" };
function ti(e, t, n, a, l, c) {
  return s(), o("div", null, [
    r("div", za, [
      I(r("input", {
        type: "text",
        placeholder: "Find an item",
        onKeyup: t[0] || (t[0] = (...i) => e.filterData && e.filterData(...i)),
        "onUpdate:modelValue": t[1] || (t[1] = (i) => e.filter = i)
      }, null, 544), [
        [W, e.filter]
      ]),
      r("div", null, [
        r("label", Ya, [
          p(" Show "),
          I(r("select", {
            "onUpdate:modelValue": t[2] || (t[2] = (i) => e.pageSize = i),
            onChange: t[3] || (t[3] = (...i) => e.filterData && e.filterData(...i))
          }, Ka, 544), [
            [se, e.pageSize]
          ]),
          p(" Records ")
        ])
      ])
    ]),
    Ga,
    r("table", null, [
      r("thead", null, [
        r("tr", null, [
          (s(!0), o($, null, v(e.internalSchema, (i) => (s(), o("th", {
            key: i.propertyName
          }, h(i.displayName), 1))), 128))
        ])
      ]),
      r("tbody", null, [
        (s(!0), o($, null, v(e.internalModel, (i) => (s(), o("tr", {
          key: i.id
        }, [
          (s(!0), o($, null, v(e.internalSchema, (u) => (s(), o("td", {
            key: u.propertyName
          }, [
            r("a", {
              onClick: (m) => e.entitySelected(i)
            }, h(i[u.propertyName]), 9, _a)
          ]))), 128))
        ]))), 128))
      ])
    ]),
    e.debug ? (s(), o("div", Qa, [
      r("div", Ja, [
        Wa,
        r("div", Za, [
          r("pre", null, "                        " + h(e.internalSchema) + `
                    `, 1)
        ])
      ]),
      r("div", Xa, [
        xa,
        r("div", ei, [
          r("pre", null, "                        " + h(e.internalModel) + `
                    `, 1)
        ])
      ])
    ])) : d("", !0)
  ]);
}
const ni = /* @__PURE__ */ S(Ua, [["render", ti], ["__scopeId", "data-v-f557b045"]]), ai = b.defineComponent({
  name: "data-editor-component",
  components: {
    listing: ni,
    "mithril-form": ge
  },
  data: function() {
    return {
      entities: [],
      mode: "listing",
      currentEntity: null,
      entitiesQuery: `query($entityType: String, $pageSize: Int, $page: Int, $sortField: String, $sortAscending: Boolean, $filter: String) {
                    entities(entityType: $entityType, pageSize: $pageSize, page: $page, sortField: $sortField, sortAscending: $sortAscending, filter: $filter)
                }`,
      currentFilter: new ye("", 10, 0, "", !1),
      currentRequest: null,
      entityQuery: `query($entityType: String, $id: Long) {
                    entity(entityType: $entityType, id: $id)
                }`
    };
  },
  methods: {
    // edits an entity
    // entity: the entity to edit
    editEntity: async function(e) {
      let t = this;
      f.debug("Editing entity", e), await w.post("/api/query", {
        query: t.entityQuery,
        variables: {
          entityType: t.schema.dataType,
          id: e.iD
        }
      }).onSuccess((a) => {
        f.debug("Entity loaded successfully", a.data.entity), t.currentEntity = a.data.entity, this.mode = "editor";
      }).send();
    },
    // filter event handler
    // event: the event that triggered the filter
    // filter: the filter event
    filter: function(e, t) {
      this.currentFilter = t, this.loadData();
    },
    // loads the data
    loadData: z(async function() {
      let e = this;
      e.currentRequest != null && (e.cancellationToken.canceled = !0), e.cancellationToken = new Oe(), e.currentRequest = w.post("/api/query", {
        query: e.entitiesQuery,
        variables: {
          entityType: e.schema.dataType,
          pageSize: Number.parseInt(e.currentFilter.pageSize),
          page: Number.parseInt(e.currentFilter.page),
          sortField: e.currentFilter.sortField,
          sortAscending: e.currentFilter.sortAscending,
          filter: e.currentFilter.filter
        }
      }).withCancellationToken(e.cancellationToken).onSuccess((t) => {
        f.debug("Entities loaded successfully", t.data.entities), e.entities = t.data.entities;
      }), await e.currentRequest.send();
    }, 100),
    // opens the listing
    openListing: function() {
      f.debug("Opening listing"), this.mode = "listing", this.loadData();
    },
    // saves an entity
    // event: the event that triggered the save
    // entity: the entity to save
    saveEntity: function(e, t) {
      let n = this;
      if (e.preventDefault(), t != null)
        return this.currentEntity = null, f.debug("Saving entity:", t), w.post("/api/command/v1/SaveModelCommand", {
          data: t,
          entityType: n.schema.dataType,
          id: t.iD
        }).onSuccess((a) => {
          f.debug("Entity saved successfully", a.data), n.mode = "listing", n.loadData();
        }).onError((a) => f.error("Error saving entity", a)).send(), !1;
    }
  },
  props: {
    name: {
      type: String,
      default: ""
    },
    schema: {
      type: Object,
      default: {}
    },
    debug: {
      type: Boolean,
      default: !0
    }
  },
  // create event (load data)
  created: function() {
    this.openListing(), this.loadData();
  },
  watch: {
    // watch for changes to the schema
    schema: function() {
      this.openListing(), this.loadData();
    }
  }
});
const ee = (e) => (N("data-v-c4c98bca"), e = e(), P(), e), ii = ["title"], ri = /* @__PURE__ */ ee(() => /* @__PURE__ */ r("span", { class: "fas fa-plus right" }, null, -1)), si = [
  ri
], oi = /* @__PURE__ */ ee(() => /* @__PURE__ */ r("span", { class: "fas fa-list right" }, null, -1)), li = [
  oi
], di = { class: "body" }, ci = { key: 0 }, ui = { key: 1 }, hi = {
  key: 2,
  class: "panel debug"
}, mi = /* @__PURE__ */ ee(() => /* @__PURE__ */ r("header", null, "Editor Schema", -1));
function fi(e, t, n, a, l, c) {
  const i = O("listing"), u = O("mithril-form");
  return s(), o("div", {
    class: k(["panel", e.schema.class])
  }, [
    r("header", null, [
      p(h(e.name) + " ", 1),
      e.mode == "listing" ? (s(), o("a", {
        key: 0,
        onClick: t[0] || (t[0] = (m) => e.editEntity({ iD: 0 })),
        title: "Add New " + e.name
      }, si, 8, ii)) : d("", !0),
      e.mode == "editor" ? (s(), o("a", {
        key: 1,
        onClick: t[1] || (t[1] = (...m) => e.openListing && e.openListing(...m)),
        title: "Show Listing"
      }, li)) : d("", !0)
    ]),
    r("div", di, [
      e.mode == "listing" ? (s(), o("div", ci, [
        L(i, {
          schema: e.schema.modelSchema,
          model: e.entities,
          debug: e.debug,
          onEntitySelected: e.editEntity,
          onFilter: e.filter
        }, null, 8, ["schema", "model", "debug", "onEntitySelected", "onFilter"])
      ])) : d("", !0),
      e.mode == "editor" ? (s(), o("div", ui, [
        L(u, {
          schema: e.schema.modelSchema,
          model: e.currentEntity,
          debug: e.debug,
          onSubmit: e.saveEntity
        }, null, 8, ["schema", "model", "debug", "onSubmit"])
      ])) : d("", !0),
      e.debug && e.schema ? (s(), o("div", hi, [
        mi,
        r("pre", null, "" + h(e.schema) + `
                `, 1)
      ])) : d("", !0)
    ])
  ], 2);
}
const pi = /* @__PURE__ */ S(ai, [["render", fi], ["__scopeId", "data-v-c4c98bca"]]), gi = b.defineComponent({
  name: "admin-application",
  computed: {
    groupedEditors: function() {
      const e = {};
      return this.sortedEditors.forEach((t) => {
        e[t.category] || (e[t.category] = []), e[t.category].push(t);
      }), Object.keys(e).map((t) => ({
        category: t,
        editors: e[t]
      }));
    },
    sortedEditors: function() {
      return this.editors.sort((e, t) => e.category < t.category ? -1 : e.category > t.category ? 1 : e.name < t.name ? -1 : e.name > t.name ? 1 : 0);
    }
  },
  data: function() {
    return {
      editors: [],
      currentEditor: null
    };
  },
  methods: {
    // close the current editor
    close: function() {
      this.editorSelected(null);
    },
    // editor selected
    // editor: the editor that was selected
    editorSelected: function(e) {
      this.debug && f.debug("Switching to editor:", (e == null ? void 0 : e.name) ?? ""), this.currentEditor = e;
    },
    // Loads the editors from the server
    loadEditors: function() {
      this.debug && f.debug("Loading Editors");
      let e = this;
      w.post("/api/query", {
        query: `query{
  editors {
    category
    componentDefinition {
      name
      schema
      scriptFile
    }
    description
    icon
    name
  }
}`
      }).onSuccess((t) => {
        e.editors = t.data.editors, e.debug && f.debug("Finished loading editors:", t.data.editors);
      }).withStorageMode(j.StorageAndUpdate).send();
    }
  },
  props: {
    debug: {
      type: Boolean,
      default: !0
    }
  },
  // created event (loads the editors)
  created: function() {
    this.loadEditors();
  }
});
const yi = (e) => (N("data-v-280d2014"), e = e(), P(), e), bi = { class: "main-application" }, Si = { class: "left-nav panel" }, $i = { class: "menu" }, vi = ["onClick", "title"], ki = { class: "editor-content" }, wi = {
  key: 1,
  class: "panel debug"
}, Mi = /* @__PURE__ */ yi(() => /* @__PURE__ */ r("header", null, "Selected Editor Info", -1));
function Ci(e, t, n, a, l, c) {
  return s(), o("div", bi, [
    r("div", Si, [
      (s(!0), o($, null, v(e.groupedEditors, (i) => (s(), o("section", {
        key: i.category
      }, [
        r("header", null, h(e.$filters.capitalize(i.category)), 1),
        r("ul", $i, [
          (s(!0), o($, null, v(i.editors, (u, m) => (s(), o("li", {
            key: u.name
          }, [
            r("a", {
              href: "#!",
              onClick: y((g) => e.editorSelected(u), ["stop", "prevent"]),
              title: u.description,
              class: k({ selected: e.currentEditor == u })
            }, [
              r("span", {
                class: k(u.icon)
              }, null, 2),
              p(h(u.name), 1)
            ], 10, vi)
          ]))), 128))
        ])
      ]))), 128))
    ]),
    r("div", ki, [
      e.currentEditor ? (s(), q(V(e.currentEditor.componentDefinition.schema.type), {
        key: 0,
        schema: e.currentEditor.componentDefinition.schema,
        name: e.currentEditor.name,
        debug: e.debug,
        onError: e.error,
        onClose: e.close
      }, null, 40, ["schema", "name", "debug", "onError", "onClose"])) : d("", !0),
      e.debug && e.currentEditor ? (s(), o("div", wi, [
        Mi,
        r("ul", null, [
          r("li", null, " Component Name: " + h(e.currentEditor.componentDefinition.name), 1),
          r("li", null, " Component Type: " + h(e.currentEditor.componentDefinition.schema.type), 1)
        ])
      ])) : d("", !0)
    ])
  ]);
}
const Ti = /* @__PURE__ */ S(gi, [["render", Ci], ["__scopeId", "data-v-280d2014"]]);
let E = {
  mounted: function(e, t) {
    if (E.onEventBound = E.onEvent.bind({ el: e }), document.addEventListener("click", E.onEventBound), typeof t.value != "function")
      throw new Error("Argument must be a function");
    E.cb = t.value;
  },
  cb: function(e) {
  },
  onEvent: function(e) {
    e.target === this.el || this.el.contains(e.target) || !E.cb || E.cb(e);
  },
  onEventBound: function() {
  },
  unbind: function() {
    document.removeEventListener("click", E.onEventBound);
  },
  update: function(e, t) {
    if (typeof t.value != "function")
      throw new Error("Argument must be a function");
    E.cb = t.value;
  }
};
function Ei(e) {
  return e.directive("click-outside", E), e;
}
function Di(e) {
  return e == null || (e.config.globalProperties.$filters = {
    moment: function(t, n, a) {
      return t == null ? "N/A" : (a ?? (a = "YYYY-MM-DDThh:mm:ss"), n ?? (n = "M-D-YYYY h:mm A"), J(t, a).format(n));
    },
    capitalize: function(t) {
      return t == null ? "" : t.charAt(0).toUpperCase() + t.slice(1);
    },
    maxsize: function(t, n, a) {
      return t == null ? "" : t.length <= n ? t : (a ?? (a = "..."), t.substring(0, n) + a);
    },
    currency: function(t, n, a) {
      return t == null ? "" : (n ?? (n = "en-US"), a ?? (a = { style: "currency", currency: "USD" }), Intl.NumberFormat(n, a).format(t));
    }
  }), e;
}
class re {
}
const Fi = b.defineComponent({
  name: "DataHandler",
  data: function() {
    return {
      count: 0,
      timer: 0,
      internalData: this.model
    };
  },
  props: {
    model: {
      type: Object,
      default: {}
    },
    schema: {
      type: re,
      default: new re()
    }
  },
  methods: {
    fetchData: function() {
      if (!this.schema.loadUrl)
        return;
      let e = this;
      w.post("/api/query", {
        query: e.schema.datalistQuery
      }).onSuccess((t) => {
        e.schema.datalist = t.data.dropDown;
      }).withStorageMode(j.StorageAndUpdate).send();
    },
    saveData: function() {
    },
    deleteData: function() {
    }
  },
  created: function() {
    if (!this.schema.datalistQuery)
      return;
    this.fetchData();
  },
  watch: {
    model: function(e, t) {
      t !== e && (this.internalModel = e);
    }
  }
});
function Ii(e, t, n, a, l, c) {
  return s(), o("div", null, [
    p(" ASDF "),
    (s(!0), o($, null, v(e.schema.children, (i) => (s(), q(V(i.type), {
      key: i.type,
      data: e.internalData,
      schema: i,
      onSave: e.saveData,
      onDelete: e.deleteData,
      onLoad: e.fetchData
    }, null, 40, ["data", "schema", "onSave", "onDelete", "onLoad"]))), 128)),
    r("pre", null, h(e.internalData), 1)
  ]);
}
const qi = /* @__PURE__ */ S(Fi, [["render", Ii]]), Vi = b.defineComponent({
  data: function() {
    return {
      items: this.items,
      showDropDown: !1
    };
  },
  props: {
    items: Array
  },
  methods: {
    showDropDownFunc: function() {
      this.showDropDown = !0;
    },
    hideDropDownFunc: function() {
      this.showDropDown = !1;
    }
  },
  directives: {
    clickOutside: E
  }
}), ji = { class: "unstyled" }, Ai = { class: "grouped" }, Oi = ["href"];
function Li(e, t, n, a, l, c) {
  const i = De("click-outside");
  return I((s(), o("ul", ji, [
    r("li", null, [
      r("a", {
        class: "fas fa-circle-user",
        href: "#",
        onClick: t[0] || (t[0] = y((...u) => e.showDropDownFunc && e.showDropDownFunc(...u), ["stop", "prevent"]))
      }),
      r("div", {
        class: k({ show: e.showDropDown, "z-depth-2": e.showDropDown, hidden: !e.showDropDown })
      }, [
        r("ul", Ai, [
          (s(!0), o($, null, v(e.items, (u) => (s(), o("li", {
            key: u.display
          }, [
            r("a", {
              href: u.url,
              class: k([u.icon])
            }, h(u.display), 11, Oi)
          ]))), 128))
        ])
      ], 2)
    ])
  ])), [
    [i, e.hideDropDownFunc]
  ]);
}
const Ni = /* @__PURE__ */ S(Vi, [["render", Li]]), Pi = (e) => {
  e.component("mithril-data-handler", qi), e.component("mithril-side-menu", Ni);
}, Bi = {
  install: (e, t) => {
    Di(e), Ei(e), Pi(e);
  }
};
class Ri {
  // constructor
  constructor() {
    // Admin application
    ae(this, "AdminApp");
    f.debug("Admin loading"), this.AdminApp = this.SetupComponents(b.createApp({
      data: function() {
        return {};
      }
    })), this.AdminApp.mount("#AdminApplication"), f.debug("Admin loaded");
  }
  // Sets up Vue components
  SetupComponents(t) {
    return t.use(Bi), t.component("admin-application", Ti), t.component("settings-editor-component", Ra), t.component("data-editor-component", pi), t;
  }
}
const Ki = (() => {
  new Ri();
})();
export {
  Ki as default
};
